# 版本与进度文档

本文是 `clinical-data-system` 的唯一进度源，用于记录版本命名、阶段边界、验收状态、当前风险和下一步计划。

## 1. 版本命名

### 1.1 对外版本

正式版本采用四段式：

```text
主版本.次版本.修订版本.构建号
```

示例：

- `1.0.0.x`：V1 基本框架和首个交付基线。
- `2.0.0.x`：V2 PDF 在线审阅和整改闭环。
- `2.1.0.x`：V2.1 审核工作台 UI 优化。
- `3.0.0.x`：V3-P0 对应的资料包切分闭环基线。
- `3.1.0.x`：V3.1，对应资料分类、板块体系和资料包审核布局重塑。

### 1.2 内部阶段

V3 早期使用内部阶段号，V3-P0 结束后进入 V3.1：

| 阶段 | 完整版本号段 | 状态 | 定位 |
| --- | --- | --- | --- |
| `V3-P0` | `3.0.0-p0.x` | 已完成 | PDF 资料包智能识别、切分、人工校正和确认入库闭环 |
| `V3.1.0` | `3.1.0.x` | 进行中 | 数据看板工作台：实验项目、整体进度预警、重要紧急事项 |
| `V3.1.x` | `3.1.x.x` | 后续 | 重塑资料分类、板块体系和资料包审核布局 |
| 字段结构化抽取 | 待定 | 后置 | 在 V3.1 分类体系稳定后启动 |

`010001.pdf` 暴露的问题不再作为 V3-P0 未完成项处理；当前判断是分类和板块口径不准确，需要进入 V3.1 重塑。

## 2. 阶段总览

| 版本/阶段 | 状态 | 完成内容 | 当前边界 |
| --- | --- | --- | --- |
| `V1` | 已完成 | 框架、主数据、资料上传、OCR、权限、Linux GPU OCR 基线 | 不包含 PDF 批注整改闭环 |
| `V1.x` | 已发生 | OCR 离线包、Linux 部署、超时假失败修复、生产文档补齐 | 不改变主业务流程 |
| `V2` | 已完成 | PDF 在线预览、画框批注、整改任务单、重传、复审、日志 | 不包含导出带批注 PDF 和统计看板 |
| `V2.1` | 已完成 | 资料项页面工作台化、文件栏/审阅栏拆分、备注自动保存、更新记录 | 不新增不必要业务表 |
| `V2.0.1` | 已完成 | PDF.js worker MIME 和浏览器兼容性修复 | 只修复 PDF 审阅兼容性 |
| `V3-P0` | 已完成 | 资料包页级识别、切分、人工拆分/合并/修正、识别原因、确认入库 | 010001 偏差归因为分类/板块体系问题，不归因为 P0 闭环失败 |
| `V3.1.0` | 进行中 | 数据看板工作台、实验项目 7 张业务表、整体进度预警、重要紧急事项 | 不做飞书实时同步，不改 OCR/PDF 资料包主流程 |
| `V3.1.x` | 后续 | 重塑资料分类、板块体系和资料包审核布局 | 具体资料和分类方案待整理 |
| 字段结构化抽取 | 后置 | 重点资料项字段抽取、字段审核和字段沉淀 | 等 V3.1 分类体系稳定后再启动 |

## 3. V1 记录

V1 的目标是形成可运行、可部署、可继续迭代的系统基线。

已完成：

- 前端 React/Vite 应用框架。
- 后端 FastAPI/API 框架。
- PostgreSQL、SQLAlchemy、Alembic。
- 用户、角色、权限。
- 项目、中心、阶段、模板、字典等基础主数据。
- 临床资料上传和资料项管理。
- 文件版本、基础审核记录和操作日志。
- PDF 资料包上传、OCR 识别和初步拆分。
- Mac 本地开发环境与 Linux 生产交付环境分工。
- Linux GPU OCR 生产部署基线。

V1 生产 OCR 基线：

- `PaddlePaddle GPU 3.3.1`
- `PaddleOCR 3.3.1`
- CUDA wheel 源：`cu129`
- Linux 生产 OCR 设备：`gpu:0`
- 生产交付方式：离线镜像包 + 离线模型缓存包

Mac 本地开发 OCR：

- 使用 Apple Vision OCR 服务。
- 默认端口 `8048`。
- 只用于开发和样本调试，不替代 Linux GPU OCR 生产基线。

## 4. V2 记录

V2 的目标是完成“审阅 PDF -> 标记问题 -> 生成任务 -> 上传整改 -> 复审关闭”的闭环。

已完成：

- PDF 在线预览。
- PDF 页面画框批注。
- 批注保存、刷新和回显。
- 根据批注生成整改任务单。
- 上传人根据任务重新上传整改文件。
- 整改文件生成新的文件版本。
- 审核人复审通过或再次退回。
- 整改任务列表和整改任务详情页。
- 全链路操作日志和版本追溯。

关键决策：

- 批注对象绑定拆分入库后的 `files` / `file_versions`。
- PDF 批注结构化保存到 `pdf_annotations`。
- 坐标使用 0-1 归一化坐标，避免缩放偏移。
- 整改任务采用原任务循环，复审不通过时同一任务再次退回。
- 资料包 `pdf_packets` 继续负责上传、OCR 和拆分，不承载 V2 批注闭环。

验收记录：

- 后端 PDF 审阅闭环测试通过。
- 后端全量测试通过。
- 前端 lint 和 build 通过。
- Safari、Chrome、夸克均完成 PDF 审阅生产构建复测。

## 5. V2.1 记录

V2.1 的目标是将受试者资料项页面从“系统字段堆叠表”调整为“临床资料审核工作台”。

已完成：

- 表格重排为“数据项 / 上传人 / 审核人 / 必填 / 上传状态 / 审核状态 / 文件 / 审阅 / 更新记录 / 备注”。
- 隐藏编码、更新时间、完整性和旧操作列。
- 文件栏只保留上传、文件名、版本大小、下载、历史、重新上传、删除。
- 文件历史改为弹窗。
- 审阅栏独立展示在线审阅和任务单状态。
- 更新记录合并上传、重新上传、审核、批注、任务、备注更新。
- 备注自动保存。
- 图标按钮 tooltip、`aria-label` 和 `title` 补齐。

## 6. V3-P0 记录

V3-P0 的目标是先把 PDF 资料包切分准，而不是提前进入字段抽取。

已完成能力：

- 页面级资料类型识别。
- OCR 文本标准化。
- 文档类型注册表。
- 标题强识别和强标题切段。
- 负向规则和互斥规则。
- 连续页资料项合并。
- 本地 debug JSON 和 `analysis-report`。
- 默认重新识别保护人工确认、人工修改和已上传片段。
- 强制重新识别覆盖人工结果。
- 人工拆分、合并、页码修正、资料项修正、确认冻结。
- 前端资料包页面工作台化。

`010005.pdf` 当前验收状态：

- 用户实际测试：通过。
- 当前基准：`12 segments, 27 text/OCR pages`。
- 人工闭环：拆分、合并、确认冻结、解除确认、默认重新识别保留人工结果、强制重新识别覆盖人工结果均通过。
- 边界校验：页码重叠、断裂/重叠拆分、非连续合并、已上传片段参与合并均能拦截。
- `analysis-report` 可返回页级 `raw_text`、`normalized_text`、`head_lines`、`tail_lines` 和 segment `reason`。
- V3-P0 已验收结束。

当前 `010005.pdf` 切分基准：

```text
1-1    知情同意书                         -> 知情同意书
2-2    知情同意书交接记录表               -> 知情同意书交接表
3-3    医学影像检查报告单                 -> CT报告
4-12   门诊病历                           -> HIS记录
13-15  入组审核/入组评估相关材料           -> 入组审核记录表
16-16  生命体征记录表                     -> 生命体征记录表
17-17  舒适度评价表                       -> 舒适度评价表
18-21  图像质量评价表                     -> 图像质量评价表
22-23  设备常用功能/设备稳定性             -> 其他次要指标评价表
24-24  其他次要标准评价表/胶囊内镜报告信息  -> 其他次要指标评价表
25-26  独立评估人检查图像质量评估表         -> 中心阅片评价结果表
27-27  胶囊内镜报告                       -> 胶囊内镜报告
```

### 6.1 `010001.pdf` 结论

`010001.pdf` 已完成观察验证。它没有推翻 V3-P0 的系统闭环，暴露的问题是现有资料分类和板块体系不准确。

当前结论：

- `010001.pdf` 不准确不是系统能力失败。
- 偏差主要来自前期资料分类、资料板块和审核布局定义不足。
- 不继续在 V3-P0 内用零碎关键词修补解决。
- 下一阶段进入 `V3.1`，先重塑分类与布局。

## 7. 当前发现的问题

2026-05-21 至 2026-05-22 测试反馈：

识别问题不只是“某一页 OCR 或分类不准”，更核心的问题是前期资料分类、板块/分组和审核布局定义不准确。

这意味着后续判断要从三层拆开：

| 层级 | 含义 | 当前动作 |
| --- | --- | --- |
| 板块 | 临床审核视角下的一组资料范围 | 需要重新重点观察和收敛 |
| 资料项 | 系统入库和审核使用的标准项 | 应与板块对应，但不强行一页一项 |
| 片段 | PDF 资料包切分出来的连续页范围 | 可由人工拆分/合并修正 |

当前优先级：

1. 先确认板块定义是否合理。
2. 再确认资料项映射是否合理。
3. 最后才调分类规则和 OCR 标准化细节。

## 8. 下一步计划

### V3-P0 收口状态

`V3-P0` 已结束，不再继续作为当前开发阶段推进。

完成结论：

- PDF 资料包切分闭环已通过。
- 人工校正、确认和上传链路已通过。
- `010001.pdf` 暴露的是分类/板块体系问题，不是系统闭环问题。
- 不在 P0 内继续靠零散关键词修补分类。

### V3.1.0 数据看板工作台

V3.1.0 的主要内容：

1. 首页 `/` 数据看板升级为临床项目工作台。
2. 一级板块分为“实验项目”和“整体进度计划达成情况”。
3. 实验项目包含 7 个子视图：进度甘特图、入组计划表、整体情况表、器械交接记录表、受试者结果统计表、临床事件记录、器械问题记录表。
4. 整体进度包含 2 个子视图：预期偏离预警、重要紧急事项完成。
5. 数据以系统内维护为主，飞书 `C200CN临床情况一览表` 只作为字段和样式参考，不作为运行时依赖。
6. 支持列表、项目/中心筛选、新增、编辑、删除、Excel 模板、导入和导出。
7. 预警规则固定为：计划日期早于今天且未完成为预警；7 天内到期且未完成为临近。

验收口径：

- `npm run build` 通过。
- Alembic migration 可升级。
- V3.1 看板 8 类数据 CRUD 可用。
- 只读用户不能写入，项目/中心范围隔离有效。
- Excel 模板、导入、导出可用，重复导入按业务唯一键更新。
- 新增逾期未完成里程碑后出现预警，完成后预警消失。

### V3.1 后续分类与布局方向

V3.1 的主要内容：

1. 重塑资料分类体系。
2. 重塑资料板块/分组体系。
3. 重新梳理板块、资料项、片段、字段之间的关系。
4. 调整资料包审核布局，使页面围绕板块和人工审核流程组织。
5. 用户整理具体资料后，再进入详细方案设计。

字段结构化抽取后置到 V3.1 分类和布局稳定之后。

## 9. 验证命令

后端：

```bash
cd backend
.venv/bin/python -m pytest
.venv/bin/python -m ruff check .
.venv/bin/alembic current
.venv/bin/alembic check
```

前端：

```bash
cd frontend
npm run lint
npm run build
```

V3-P0 定向验证：

```bash
cd backend
.venv/bin/python -m pytest tests/test_pdf_packets.py
cd ../frontend
npm run build
```

## 10. 生产变更原则

- 生产变更前必须备份数据库和文件存储。
- 数据库结构变更必须走 Alembic migration。
- 后端 Python 文件替换后必须重启 backend。
- 前端静态资源或镜像替换后必须重启前端/Nginx 或确认资源刷新。
- OCR 服务代码或镜像变化必须单独验证 `/health` 和真实样本。
- Mac Apple Vision OCR 只用于本地开发，不替代 Linux GPU OCR 生产基线。
