# 临床数据收集系统 UI 修改方案 V2.1

## 1. 版本定位

本方案用于优化当前“临床数据集详情页”的表格与文件操作 UI。

当前系统已经具备以下基础能力：

- 数据集详情展示
- 临床资料项列表
- PDF 上传
- 拆分后 PDF 文件展示
- 文件下载、预览、重新上传、删除
- 审核状态展示
- 审核记录查看
- 自动保存备注能力

但当前页面存在几个明显问题：

1. 表格字段过多，横向空间拥挤。
2. 部分字段属于系统内部字段，不适合展示给临床用户。
3. 文件区同时展示拖拽上传、上传按钮和多个图标，视觉复杂。
4. 备注框过大且位置靠前，影响主流程阅读。
5. 审核记录与更新时间信息重复。
6. 操作栏功能较弱，且与自动保存逻辑冲突。
7. 后续要接入“在线审阅 + 批注 + 任务单”功能，需要提前整理入口。

V2.1 的核心目标是：

> 让页面从“系统字段堆叠表”调整为“临床资料审核工作台”。

## 2. 核心修改原则

### 2.1 面向临床用户，而不是面向数据库字段

临床用户主要关心：

- 这是什么资料
- 谁上传
- 谁审核
- 是否必填
- 上传了吗
- 审核通过了吗
- 文件在哪里
- 能不能审阅
- 有没有整改任务
- 最近发生了什么
- 有没有备注

临床用户不需要直接看到：

- 编码
- 完整性计算字段
- 后端状态聚合字段
- 重复更新时间
- 仅用于系统识别的字段

因此，本次优化会隐藏“编码”和“完整性”列，将其保留为后端逻辑字段。

### 2.2 文件资产和审阅协作分离

当前文件区混合了承载文件本身、上传、预览、审阅、记录等操作，后续继续增加批注和任务单后会更加混乱。

V2.1 中明确拆分为两个区域：

```text
文件栏：负责文件资产操作
审阅栏：负责在线审阅、批注、任务单
```

也就是：

```text
文件 = 下载 / 历史 / 重新上传 / 删除
审阅 = 在线审阅 / 任务单
```

### 2.3 表格只展示关键状态，复杂信息进入弹窗或详情页

表格行内只展示最关键的信息。复杂内容进入：

- 文件历史弹窗
- 在线审阅页
- 任务单弹窗 / 页面
- 更新记录弹窗

这样表格不会变成按钮动物园，也方便后续扩展。

### 2.4 原文件不动，版本与操作全程可追溯

重新上传不覆盖历史文件。

文件、审阅、任务、更新记录都必须围绕版本链路展开：

```text
v1 上传 → 审核退回 → 批注 / 任务单 → v2 重新上传 → 复审通过
```

## 3. 当前问题整理

### 3.1 编码列不适合展示

当前页面展示“编码”列，但临床部门实际不会根据编码判断资料。编码主要用于：

- 后端唯一识别
- 数据导入导出
- 资料项排序
- PDF 拆分规则匹配
- 文件、批注、任务单关联

因此，编码应作为系统字段保留，不在普通表格中展示。

### 3.2 完整性列不适合展示

当前“完整性”列展示“资料齐全 / 资料不全”，但这类字段更适合作为系统内部聚合判断，不适合每一行展示。

原因：

1. 容易与上传状态、审核状态重复。
2. 用户更关心“当前要不要补充、是否被驳回、是否待复审”。
3. 完整性更适合作为顶部总状态或后端规则判断。

### 3.3 备注框过大且位置靠前

当前备注输入框高度较大，放在文件和审核前面，打断了用户阅读主流程。

备注应该是辅助信息，建议放在最后一列，并使用小型自动保存输入框。

### 3.4 文件区过于复杂

当前文件区同时包含：

- 拖入上传区域
- 文件类型选择
- 上传按钮
- 文件卡片
- 下载
- 预览
- 编辑 / 审阅
- 记录
- 重新上传
- 删除

这导致单行高度过高，用户需要识别太多按钮。

### 3.5 审核记录与更新时间重复

当前页面同时有“更新时间”和审核记录中的“最近：通过 / 驳回 + 日期”。

建议合并为“更新记录”，统一承载最近关键操作。

### 3.6 操作栏没有必要保留

当前右侧操作栏主要是保存备注，但备注已经可以自动保存。操作栏会占用额外空间，也不利于页面简洁。

## 4. V2.1 最终表格结构

### 4.1 推荐字段顺序

V2.1 表格最终字段为：

```text
数据项 | 上传人 | 审核人 | 必填 | 上传状态 | 审核状态 | 文件 | 审阅 | 更新记录 | 备注
```

### 4.2 删除 / 隐藏字段

从前端表格移除以下列：

```text
编码
更新时间
完整性
操作
```

说明：

| 字段 | 前端处理 | 后端处理 |
|---|---|---|
| 编码 | 不展示 | 保留，用于系统识别、排序、关联 |
| 更新时间 | 不单独展示 | 合并进“更新记录” |
| 完整性 | 不展示 | 保留，用于系统内部判断 |
| 操作 | 删除 | 相关操作分散到文件、审阅、更新记录、备注自动保存 |

## 5. 字段说明

### 5.1 数据项

展示资料项名称，例如：

```text
知情同意书
知情同意书交接表
生命体征记录表
```

要求：

- 使用清晰加粗文字。
- 如果名称过长，允许换行或 tooltip 展示全称。
- 不再展示编码。

### 5.2 上传人

展示该资料项的上传责任人。

示例：

```text
系统管理员
临床专员A
```

### 5.3 审核人

展示该资料项的审核责任人。

示例：

```text
系统管理员
项目负责人
```

### 5.4 必填

使用小标签展示是否必填。

建议：

```text
必填
非必填
```

如果大部分资料都是必填，也可以只在必填项显示标签，非必填不显示，进一步降低视觉噪音。

### 5.5 上传状态

用于表示资料上传情况。

建议状态：

```text
未上传
已上传
待补充
已重新上传
```

### 5.6 审核状态

用于表示审核流转情况。

建议状态：

```text
待审核
已通过
已驳回
待复审
再次退回
```

注意：

- “资料齐全 / 资料不全”不要放在此列。
- 资料整体是否齐全由顶部状态或后端规则判断。

### 5.7 文件

文件栏只负责文件资产，不负责审阅协作。

有文件时展示：

```text
文件名
版本号 · 文件大小
下载 | 历史 | 重新上传 | 删除
```

示例：

```text
010001-知情同意书-v1.pdf
v1 · 590.7 KB
下载 | 历史 | 重新上传 | 删除
```

无文件时展示：

```text
上传 PDF
```

“上传 PDF”按钮需要支持：

- 点击选择文件上传
- 拖拽文件到该区域上传

不要再同时展示大面积拖拽框、下拉选择和独立上传按钮。

### 5.8 审阅

新增独立“审阅”栏，用于承载在线审阅和任务单。

有文件时展示：

```text
在线审阅 | 任务单
```

无文件时：

```text
在线审阅置灰
任务单置灰或隐藏
```

任务单状态可作为角标展示：

```text
任务单 · 待整改
任务单 · 待复审
任务单 · 已关闭
```

审阅栏后续承载功能：

- 打开 PDF 在线审阅页
- 画框批注
- 查看批注列表
- 生成整改任务
- 查看任务单
- 上传人处理整改任务

### 5.9 更新记录

将原“审核记录”和“更新时间”合并为“更新记录”。

该列展示最新一条关键操作，并保留完整记录入口。

示例：

```text
记录
最近：驳回 · 2026/5/12
```

或：

```text
记录
最近：重新上传 · 2026/5/15
```

关键操作类型包括：

```text
上传
重新上传
通过
驳回
创建批注
生成整改任务
提交整改
复审通过
再次退回
删除文件
修改备注
```

点击“记录”后打开完整更新记录弹窗。

### 5.10 备注

备注列放在最后。

展示方式：

- 默认小输入框。
- 单行或较小高度，建议 32px-40px。
- 聚焦后可展开为多行。
- 新增自动保存功能。
- 不再需要“保存”按钮。

当前实测情况：

```text
备注暂未实现自动更新 / 自动保存能力。
```

因此 V2.1 中需要明确新增备注自动保存功能，而不是只调整 UI。

自动保存逻辑：

```text
输入停止 800ms 后自动保存
失焦时再次触发保存
保存中显示“保存中...”
成功显示“已保存”
失败显示“保存失败，请重试”
```

交互要求：

1. 用户修改备注后，不需要点击保存按钮。
2. 输入停止约 800ms 后自动提交到后端。
3. 如果用户输入后直接离开输入框，失焦时也必须触发保存。
4. 保存成功后，更新当前行的更新记录，可记录为“修改备注”。
5. 如果保存失败，备注内容不要直接丢失，应提示用户重试。
6. 如果连续快速输入，应防抖处理，只提交最后一次内容。
7. 如果备注内容与原内容一致，不重复提交。

后端需要支持备注更新接口。如果已有备注保存接口，可直接复用；如果没有，需要新增单独接口。

建议接口：

```http
PATCH /api/clinical-data/items/{item_id}/remark
```

请求示例：

```json
{
  "remark": "此资料需补充签名页"
}
```

响应示例：

```json
{
  "success": true,
  "remark": "此资料需补充签名页",
  "updated_at": "2026-05-12 10:30:00"
}
```

## 6. 文件栏交互设计

### 6.1 文件栏按钮样式

文件栏的小图标按钮可以保留当前设计，不强制改成文字按钮。

但所有图标按钮必须增加清晰、灵敏的 hover tooltip，确保用户鼠标移上去后能快速知道按钮含义。

文件栏保留的小图标按钮：

```text
下载
历史
重新上传
删除
```

Tooltip 要求：

1. 鼠标悬停后快速显示，建议延迟 100ms-200ms，不要太慢。
2. Tooltip 文案必须明确，不使用模糊词。
3. Tooltip 位置不要遮挡按钮本身和文件名。
4. 图标按钮必须有 aria-label 或 title，方便可访问性和浏览器原生提示兜底。
5. 所有文件栏按钮的 tooltip 触发速度和样式保持一致。

建议 tooltip 文案：

| 图标按钮 | Tooltip 文案 |
|---|---|
| 下载 | 下载当前文件 |
| 历史 | 查看历史版本 |
| 重新上传 | 重新上传并生成新版本 |
| 删除 | 删除当前文件 |

### 6.2 无文件状态

展示轻量上传入口：

```text
上传 PDF
```

交互：

1. 点击按钮打开文件选择器。
2. 拖拽 PDF 到该区域也可上传。
3. 上传完成后生成 v1 文件。
4. 更新上传状态为“已上传”。
5. 更新记录新增“上传 · 时间”。

### 6.3 有文件状态

展示文件卡片：

```text
文件名
v1 · 590.7 KB
小图标：下载 | 历史 | 重新上传 | 删除
```

按钮说明：

| 操作 | 功能 |
|---|---|
| 下载 | 下载当前版本文件 |
| 历史 | 打开文件版本历史弹窗 |
| 重新上传 | 上传新文件并生成新版本 |
| 删除 | 删除当前文件或标记删除，需二次确认 |

### 6.4 重新上传逻辑

重新上传必须生成新版本，不覆盖旧文件。

示例：

```text
v1：首次上传
v2：整改后重新上传
v3：二次整改后重新上传
```

更新记录中写入：

```text
最近：重新上传 · 2026/5/15
```

## 7. 审阅栏交互设计

### 7.1 审阅栏按钮样式

审阅栏继续保留当前小图标按钮设计，不强制改成文字按钮。

当前测试情况：

```text
在线审阅和整改任务两个小按钮已有 hover 提示，但提示出现较慢。
```

V2.1 需要优化为：

1. 在线审阅、整改任务 tooltip 保留。
2. tooltip 触发速度调快，建议延迟 100ms-200ms。
3. tooltip 文案清晰。
4. tooltip 样式与文件栏所有按钮统一。
5. 鼠标移出后 tooltip 及时消失。
6. 不要只给审阅栏两个按钮加提示，文件栏所有图标按钮也要加提示。

建议 tooltip 文案：

| 图标按钮 | Tooltip 文案 |
|---|---|
| 在线审阅 | 在线审阅 PDF |
| 整改任务 / 任务单 | 查看整改任务单 |

### 7.2 在线审阅

点击“在线审阅”后进入 PDF 审阅页面。

如果没有文件：

```text
请先上传文件后再进行审阅
```

如果有文件：

```text
打开当前版本 PDF 的在线审阅页
```

在线审阅页后续应支持：

- PDF 在线预览
- 画框批注
- 注释说明
- 批注列表
- 生成整改任务单

### 7.3 任务单

点击“任务单”后：

如果无任务：

```text
暂无整改任务
```

如果有任务：

打开任务单详情或任务单列表。

任务单入口建议显示状态：

```text
任务单 · 待整改
任务单 · 待复审
任务单 · 已关闭
```

任务单弹窗 / 页面至少展示：

- 任务状态
- 问题数量
- 关联批注
- 创建人
- 接收人
- 创建时间
- 最新处理记录
- 重新上传入口
- 复审结果

## 8. 更新记录弹窗设计

点击“记录”后展示完整更新记录。

字段建议：

```text
时间
操作人
操作类型
操作说明
关联文件版本
关联任务单
备注
```

示例：

| 时间 | 操作人 | 操作类型 | 说明 | 版本 |
|---|---|---|---|---|
| 2026/5/12 09:30 | 系统管理员 | 上传 | 上传知情同意书 | v1 |
| 2026/5/12 10:20 | 系统管理员 | 驳回 | 签名缺失，需补充 | v1 |
| 2026/5/13 14:15 | 上传人A | 重新上传 | 已补充签名页 | v2 |
| 2026/5/13 15:00 | 系统管理员 | 通过 | 复审通过 | v2 |

## 9. 顶部状态区域优化

当前数据集标题区域展示：

```text
010001
资料不全
已驳回
```

建议保留顶部状态区域，用于展示整体状态，而不是在每一行都展示完整性。

顶部区域建议展示：

- 数据集编号
- 受试者基础信息
- 整体资料状态
- 整体审核状态
- 最近更新时间 / 最近关键操作
- 刷新按钮

示例：

```text
010001
资料不全 · 已驳回
最近：知情同意书被驳回 · 2026/5/12
```

## 10. 状态标签规范

建议统一标签颜色和语义。

### 10.1 必填标签

```text
必填：黄色
非必填：灰色或不显示
```

### 10.2 上传状态

```text
未上传：灰色
已上传：绿色
待补充：橙色
已重新上传：蓝色
```

### 10.3 审核状态

```text
待审核：灰色
已通过：绿色
已驳回：红色
待复审：蓝色
再次退回：橙色
```

### 10.4 任务状态

```text
无任务：灰色
待整改：红色
待复审：蓝色
已关闭：绿色
再次退回：橙色
```

## 11. 后端字段处理原则

### 11.1 编码字段

不展示给普通用户，但继续保留。

用途：

- 资料项唯一识别
- 数据导入导出
- 资料项排序
- PDF 拆分与关键字识别关联
- 文件版本关联
- 批注关联
- 任务单关联

### 11.2 完整性字段

不在行内展示，但继续保留。

用途：

- 判断资料是否齐全
- 判断是否缺少必填项
- 判断是否存在待整改任务
- 判断是否可进入最终通过状态
- 顶部整体状态计算

### 11.3 更新时间字段

不单独作为表格列展示。

用途：

- 更新记录排序
- 顶部最近操作时间
- 完整记录弹窗

## 12. 推荐最终页面结构

### 12.1 页面整体结构

```text
顶部：返回数据集 / 数据集编号 / 整体状态 / 基本信息 / 刷新
中部：一键审批当前受试者资料
下部：阶段卡片
  └── 表格：数据项、上传人、审核人、必填、上传状态、审核状态、文件、审阅、更新记录、备注
```

### 12.2 单行示例

```text
知情同意书 | 系统管理员 | 系统管理员 | 必填 | 待补充 | 已驳回 | 010001-知情同意书-v1.pdf / 下载 历史 重新上传 删除 | 在线审阅 任务单·待整改 | 记录 / 最近：驳回·2026/5/12 | 小备注框
```

更理想的视觉表达：

```text
知情同意书
上传人：系统管理员
审核人：系统管理员
必填｜待补充｜已驳回
文件：010001-知情同意书-v1.pdf  v1 · 590.7 KB
操作：下载｜历史｜重新上传｜删除
审阅：在线审阅｜任务单·待整改
更新：最近 驳回 · 2026/5/12｜记录
备注：小输入框
```

如果表格横向空间仍然紧张，可以考虑行内信息分两层展示，但第一版建议先保持表格结构。

## 13. 给 Codex 的开发任务单

```text
任务名称：临床数据集详情页 UI 优化 V2.1

背景：
当前临床数据集详情页已经具备资料项展示、PDF 上传、文件操作、审核记录和备注保存能力。但当前表格字段过多，部分字段属于系统内部字段，不适合展示给临床用户；文件栏操作过于复杂；备注框过大且位置靠前；审核记录与更新时间重复；操作栏没有必要存在。需要对页面进行 V2.1 版 UI 优化，为后续 PDF 在线审阅、批注和整改任务单能力预留清晰入口。

目标：
在不破坏现有上传、下载、删除、审核记录、备注保存等功能的前提下，重构临床数据集详情页表格展示结构，使页面更简洁，主流程更清晰，并新增“审阅”栏作为在线审阅和任务单入口。

一、表格字段调整

将当前表格字段调整为：

数据项 | 上传人 | 审核人 | 必填 | 上传状态 | 审核状态 | 文件 | 审阅 | 更新记录 | 备注

请从前端表格中删除 / 隐藏以下列：

1. 编码
2. 更新时间
3. 完整性
4. 操作

注意：
- 编码字段不要从数据库删除，仅前端不展示。
- 完整性字段不要从数据库删除，仅前端不展示。
- 更新时间不要单独展示，合并到更新记录中。
- 操作栏删除，备注改为自动保存。

二、编码字段处理

编码字段作为系统内部字段保留，继续用于：

1. 资料项唯一识别
2. 数据导入导出
3. 资料项排序
4. PDF 拆分与关键字识别关联
5. 文件版本关联
6. 批注和任务单关联

普通用户表格中不展示编码。

三、完整性字段处理

完整性字段作为系统内部判断字段保留，继续用于：

1. 判断资料是否齐全
2. 判断是否缺少必填项
3. 判断是否存在待整改任务
4. 判断是否可进入最终通过状态
5. 顶部整体状态计算

不要在表格中单独展示“资料齐全 / 资料不全”列。

四、文件栏优化

文件栏只负责文件资产操作，不再承载审阅入口。

文件栏仅保留：

1. 当前文件信息
2. 下载
3. 历史
4. 重新上传
5. 删除

有文件时展示：

文件名
版本号 · 文件大小
下载 | 历史 | 重新上传 | 删除

无文件时展示：

上传 PDF

上传 PDF 入口要求：

1. 支持点击选择文件上传。
2. 支持拖拽文件到该区域上传。
3. 不要同时展示大面积拖拽框、文件类型下拉框和独立上传按钮。
4. 上传完成后生成文件版本。
5. 重新上传必须生成新版本，不覆盖旧版本。

五、新增审阅栏

在文件栏后新增“审阅”栏。

审阅栏包含：

1. 在线审阅
2. 任务单

显示逻辑：

如果当前有文件：
- 显示“在线审阅”按钮。
- 显示“任务单”按钮。

如果当前没有文件：
- “在线审阅”置灰。
- “任务单”置灰或隐藏。

任务单如果存在未关闭任务，显示状态角标：

任务单 · 待整改
任务单 · 待复审
任务单 · 已关闭
任务单 · 再次退回

点击在线审阅：
- 有文件时进入当前版本 PDF 在线审阅页。
- 无文件时提示“请先上传文件后再进行审阅”。

点击任务单：
- 有任务时打开任务单详情或任务列表。
- 无任务时提示“暂无整改任务”。

六、更新记录栏

将原“审核记录”和“更新时间”合并为“更新记录”。

更新记录栏展示：

1. 记录入口
2. 最近一次关键操作
3. 最近一次关键操作时间

示例：

记录
最近：驳回 · 2026/5/12

记录
最近：重新上传 · 2026/5/15

关键操作类型包括：

上传、重新上传、通过、驳回、创建批注、生成整改任务、提交整改、复审通过、再次退回、删除文件、修改备注。

点击“记录”后打开完整更新记录弹窗。

完整更新记录弹窗字段：

1. 时间
2. 操作人
3. 操作类型
4. 操作说明
5. 关联文件版本
6. 关联任务单
7. 备注

七、备注栏优化

备注栏移动到最后一列。

备注输入框要求：

1. 默认显示为小输入框，高度约 32px-40px。
2. 聚焦后可展开为多行。
3. 新增自动保存功能。当前实测备注尚未自动更新，本次必须补齐。
4. 删除右侧单独“保存”按钮。
5. 输入停止 800ms 后自动保存。
6. 失焦时再次触发保存。
7. 保存时显示“保存中...”。
8. 保存成功显示“已保存”。
9. 保存失败显示“保存失败，请重试”。
10. 连续输入时做防抖处理，只提交最后一次内容。
11. 内容无变化时不重复提交。
12. 保存成功后更新“更新记录”，操作类型可记为“修改备注”。

如果当前后端没有备注更新接口，需要新增接口，例如：

PATCH /api/clinical-data/items/{item_id}/remark

请求体：

{
  "remark": "备注内容"
}

八、按钮 Tooltip 优化

文件栏和审阅栏继续保留当前小图标按钮设计，但所有图标都必须有清晰且快速响应的 hover 提示。

需要添加 tooltip 的按钮包括：

文件栏：
1. 下载
2. 历史
3. 重新上传
4. 删除

审阅栏：
1. 在线审阅
2. 整改任务 / 任务单

Tooltip 要求：

1. 鼠标悬停后快速显示，建议延迟 100ms-200ms。
2. 当前在线审阅和整改任务的提示出现较慢，需要调快。
3. 所有图标按钮 tooltip 样式保持一致。
4. 鼠标移出后及时消失。
5. 每个图标按钮增加 aria-label 或 title，作为可访问性和原生提示兜底。
6. Tooltip 文案要明确，不要只写“查看”这种模糊词。

建议 tooltip 文案：

下载：下载当前文件
历史：查看历史版本
重新上传：重新上传并生成新版本
删除：删除当前文件
在线审阅：在线审阅 PDF
整改任务 / 任务单：查看整改任务单

九、顶部状态区域

顶部继续作为整体状态展示区域。

建议展示：

1. 数据集编号
2. 整体资料状态
3. 整体审核状态
4. 基本信息
5. 最近关键操作，可选
6. 刷新按钮

完整性状态如“资料不全”可以在顶部展示，不在每一行展示。

九、状态标签规范

请统一状态标签颜色：

必填：黄色
非必填：灰色或不显示

上传状态：
未上传：灰色
已上传：绿色
待补充：橙色
已重新上传：蓝色

审核状态：
待审核：灰色
已通过：绿色
已驳回：红色
待复审：蓝色
再次退回：橙色

任务状态：
无任务：灰色
待整改：红色
待复审：蓝色
已关闭：绿色
再次退回：橙色

十、文件历史弹窗

点击“历史”后打开文件版本历史弹窗。

弹窗字段：

1. 版本号
2. 文件名
3. 上传人
4. 上传时间
5. 文件大小
6. 状态
7. 操作

操作包括：

1. 下载
2. 查看
3. 设为当前版本，可选，需权限
4. 删除，可选，需权限

历史版本不能被新上传覆盖。

十一、验收标准

1. 表格不再展示“编码”列。
2. 表格不再展示“更新时间”列。
3. 表格不再展示“完整性”列。
4. 表格不再展示“操作”列。
5. 最终字段顺序为：数据项 | 上传人 | 审核人 | 必填 | 上传状态 | 审核状态 | 文件 | 审阅 | 更新记录 | 备注。
6. 文件栏只展示文件信息、下载、历史、重新上传、删除。
7. 上传入口融合为“上传 PDF”，支持点击和拖拽。
8. 新增“审阅”栏，包含在线审阅和任务单入口。
9. 更新记录栏显示最近关键操作和记录入口。
10. 备注栏移动到最后，使用小输入框。
11. 备注必须支持自动保存，输入停止后自动提交，失焦时也会保存。
12. 备注保存过程有“保存中 / 已保存 / 保存失败”提示。
13. 备注保存成功后，更新记录中能看到“修改备注”或对应最新操作。
14. 删除原右侧保存按钮。
15. 重新上传必须生成新版本，不覆盖旧版本。
16. 文件栏所有小图标按钮都有 hover tooltip。
17. 审阅栏在线审阅和整改任务按钮保留 hover tooltip，并将触发速度调快。
18. Tooltip 建议 100ms-200ms 内出现，鼠标移出后及时消失。
19. 所有图标按钮有 aria-label 或 title。
20. 不影响现有上传、下载、删除、审核记录能力。
21. 页面单行高度降低，横向拥挤明显改善。
22. 顶部状态区域仍能展示整体资料状态和审核状态。

## 14. 实施优先级

### P0：字段隐藏与表格重排

优先完成：

- 删除编码列
- 删除完整性列
- 删除更新时间列
- 删除操作列
- 重排字段顺序

### P1：文件栏简化

完成：

- 上传入口融合
- 文件卡片简化
- 下载、历史、重新上传、删除保留
- 预览 / 审阅从文件栏移除

### P2：新增审阅栏

完成：

- 在线审阅入口
- 任务单入口
- 无文件时置灰
- 有任务时展示任务状态

### P3：更新记录合并

完成：

- 合并更新时间和审核记录
- 展示最近关键操作
- 保留记录弹窗

### P4：备注自动保存优化

完成：

- 备注移到最后
- 小输入框
- 聚焦展开
- 新增备注自动保存接口或复用现有保存接口
- 输入停止 800ms 后自动保存
- 失焦触发保存
- 保存中 / 已保存 / 保存失败提示
- 保存成功后写入更新记录

### P5：图标按钮 Tooltip 优化

完成：

- 文件栏下载、历史、重新上传、删除按钮均增加 tooltip
- 审阅栏在线审阅、整改任务按钮保留 tooltip
- 调快 tooltip 触发速度，建议 100ms-200ms
- 统一 tooltip 样式
- 增加 aria-label 或 title

## 15. V2.1 结论

V2.1 的重点不是增加功能，而是把页面结构理顺。

最终页面应该围绕一条清晰的使用路径展开：

```text
资料项是什么 → 谁负责 → 当前上传/审核状态 → 文件在哪里 → 如何审阅和处理任务 → 最近发生了什么 → 备注补充
```

隐藏编码和完整性后，页面会更贴近临床用户的真实工作方式；拆分文件栏和审阅栏后，后续增加 PDF 在线批注、整改任务单和复审流程也会更加自然。

V2.1 完成后，系统就具备继续升级为“临床资料在线审阅与整改闭环平台”的前端基础。

