# 临床数据系统 PDF 在线批注与整改闭环技术落地计划

## 1. 项目背景

当前临床数据收集系统已经完成基础框架，具备临床资料上传、PDF 拆分、关键字分页、人工确认、审核状态管理等能力。现阶段领导提出的新需求是：拆分后的 PDF 文件能够在页面中打开并进行批注，审核人可以在页面中画框标记问题区域并填写说明，系统根据批注形成整改任务单，反馈给上传资料人员。上传人员勘误后重新上传，审核人再次复审。

该需求本质上是在现有“资料收集系统”基础上扩展“在线审阅、批注反馈、资料勘误、版本复审”的闭环能力。

## 2. 建设目标

本阶段目标不是重做临床数据系统，而是在现有系统上新增一套 PDF 审阅协作模块，形成以下闭环：

```text
资料上传 → 系统拆分 → 审核人在线审阅 → 画框批注 → 生成整改任务 → 上传人勘误重传 → 审核人复审 → 审核通过 / 再次退回
```

最终实现：

1. 原始 PDF 文件完整留存，不被破坏。
2. 审核人在页面中打开 PDF 副本或可审阅版本进行批注。
3. 批注包含画框区域、问题类型、文字说明、严重程度、处理状态。
4. 批注可自动或手动汇总为整改任务单。
5. 上传人员可查看任务单和对应批注，按要求重新上传资料。
6. 系统保留文件版本、批注记录、任务流转记录和审核记录。
7. 后续可扩展导出带批注 PDF、整改报告、质控统计看板。

## 3. 核心原则

### 3.1 原文件不可变原则

临床原始 PDF 文件必须作为原始证据留存，不直接写入批注、不覆盖、不修改。

推荐文件关系：

```text
原始文件 original.pdf
  ├── 拆分页 / 拆分文件 split_xxx.pdf
  ├── 审阅副本 review_copy_v1.pdf，可选
  └── 批注数据 annotation records，存数据库
```

批注优先采用“PDF 文件 + 数据库批注层”的方式实现，而不是直接写入 PDF 文件内部。

### 3.2 批注数据结构化原则

每一个批注都要作为结构化数据保存，包括：

- 所属文件版本
- 所属页码
- 框选区域坐标
- 批注文字
- 问题类型
- 严重程度
- 创建人
- 创建时间
- 当前处理状态

这样后续才能支持任务流转、统计分析、版本追踪和审计。

### 3.3 坐标归一化原则

PDF 在线预览时，用户可能缩放页面或使用不同屏幕。批注框不能直接保存屏幕像素坐标，而应保存归一化坐标。

示例：

```json
{
  "page_no": 3,
  "x": 0.235,
  "y": 0.412,
  "width": 0.188,
  "height": 0.073
}
```

含义是：批注框位于第 3 页，横向从页面宽度的 23.5% 开始，纵向从页面高度的 41.2% 开始，宽度占页面宽度 18.8%，高度占页面高度 7.3%。

这样无论页面放大、缩小还是换设备，批注框都能稳定还原。

### 3.4 版本可追溯原则

上传人每次重新上传，系统都不覆盖旧文件，而是生成新的文件版本。

示例：

```text
v1：上传完成，审核人批注 3 条，退回整改
v2：上传人重新上传，等待复审
v2：审核人复审通过，任务关闭
```

如果再次不通过：

```text
v1：首次上传，退回
v2：第一次整改上传，仍不通过
v3：第二次整改上传，通过
```

## 4. 推荐技术路线

### 4.1 前端技术方案

推荐采用：

```text
PDF.js + Canvas / SVG 批注层 + 右侧批注任务面板
```

前端页面结构：

```text
PDF审阅页
├── 左侧：页码缩略图 / 文件信息 / 版本切换
├── 中间：PDF预览区域
│   ├── PDF.js 渲染层
│   └── Canvas 或 SVG 透明批注层
└── 右侧：批注列表 / 任务单 / 审核操作
```

主要能力：

1. 打开拆分后的 PDF 文件。
2. 支持翻页、缩放、适应宽度。
3. 支持选择“画框批注”工具。
4. 用户拖拽鼠标画矩形框。
5. 弹出批注输入框。
6. 保存批注后，页面立即显示批注框和编号。
7. 右侧同步显示批注列表。
8. 点击批注列表可跳转到对应页码和区域。
9. 支持编辑、删除、解决、驳回批注。
10. 支持根据批注生成整改任务单。

### 4.2 后端技术方案

如果当前系统已使用 FastAPI，可以继续沿用。

推荐新增后端模块：

```text
app/modules/pdf_review/
├── router.py          # PDF审阅相关接口
├── service.py         # 批注、任务、版本核心逻辑
├── models.py          # 数据模型
├── schemas.py         # 请求响应结构
└── permissions.py     # 权限判断
```

后端需要提供的核心能力：

1. 获取 PDF 文件预览地址。
2. 获取某文件版本的批注列表。
3. 新增批注。
4. 修改批注。
5. 删除或关闭批注。
6. 根据批注创建整改任务单。
7. 查询上传人待处理任务。
8. 上传人提交整改文件。
9. 审核人复审文件。
10. 查询文件版本链路。
11. 记录操作日志。

## 5. 数据库设计

以下为建议表结构，可根据当前系统已有表进行融合，不一定要完全新建。

### 5.1 文件主表：clinical_files

如果系统已有文件表，可以在原表基础上扩展。

建议字段：

```text
id
project_id
stage_id
data_item_id
current_version_id
original_file_id
file_status
review_status
uploaded_by
created_at
updated_at
```

说明：

- clinical_files 表表示一个资料项下的文件对象。
- current_version_id 指向当前最新版本。
- original_file_id 指向原始上传文件。
- review_status 表示整体审核状态。

### 5.2 文件版本表：clinical_file_versions

用于保存每次上传、重传和审阅副本。

```text
id
clinical_file_id
version_no
file_name
file_path
file_size
file_hash
file_type
source_type
parent_version_id
uploaded_by
uploaded_at
status
remark
```

字段说明：

```text
source_type:
- original 原始文件
- split 拆分文件
- review_copy 审阅副本
- corrected_upload 整改后重新上传文件

status:
- pending_review 待审核
- reviewing 审核中
- rejected 已退回
- corrected 已整改
- approved 已通过
- archived 已归档
```

### 5.3 PDF 批注表：pdf_annotations

```text
id
clinical_file_id
file_version_id
project_id
stage_id
data_item_id
page_no
x
y
width
height
comment
issue_type
severity
status
created_by
created_at
updated_by
updated_at
resolved_by
resolved_at
```

字段说明：

```text
issue_type:
- missing_page 缺页
- wrong_page 错页
- unclear_scan 扫描不清晰
- inconsistent_info 信息不一致
- missing_signature 签名缺失
- missing_stamp 盖章缺失
- missing_date 日期缺失
- wrong_subject 受试者信息不匹配
- wrong_document 资料类型不匹配
- other 其他

severity:
- low 一般
- medium 重要
- high 严重

status:
- open 待处理
- task_created 已生成任务
- submitted 已提交整改
- resolved 已解决
- rejected 复审不通过
- closed 已关闭
```

### 5.4 整改任务单表：correction_tasks

```text
id
task_no
project_id
stage_id
data_item_id
clinical_file_id
file_version_id
title
description
created_by
assigned_to
status
due_date
created_at
updated_at
submitted_at
reviewed_at
closed_at
```

状态建议：

```text
pending 待处理
processing 处理中
submitted 已重新上传
reviewing 待复审
approved 已通过
returned 再次退回
closed 已关闭
cancelled 已取消
```

### 5.5 任务与批注关联表：correction_task_annotations

一个任务单可以包含多个批注。

```text
id
task_id
annotation_id
created_at
```

### 5.6 操作日志表：operation_logs

用于审计。

```text
id
module
object_type
object_id
action
operator_id
operator_name
before_data
after_data
created_at
ip_address
```

典型 action：

```text
create_annotation
update_annotation
delete_annotation
create_task
submit_correction
review_approved
review_returned
upload_new_version
```

## 6. 业务流程设计

### 6.1 审核人批注流程

```text
1. 审核人进入临床数据集页面
2. 点击某个拆分 PDF 文件的“查看/审阅”按钮
3. 系统打开 PDF 审阅页
4. 审核人选择“画框批注”工具
5. 在 PDF 页面中框选问题区域
6. 填写问题类型、问题说明、严重程度
7. 保存批注
8. 系统保存批注数据并在页面回显
9. 审核人继续添加多个批注
10. 点击“生成整改任务”
11. 选择接收人，默认为该资料上传人
12. 提交任务
13. 文件状态变为“已退回 / 待整改”
14. 批注状态变为“已生成任务”
```

### 6.2 上传人整改流程

```text
1. 上传人登录系统
2. 进入“我的任务”或在对应资料项看到待整改提示
3. 打开整改任务单
4. 查看任务说明和所有批注
5. 点击批注可跳转到 PDF 对应区域
6. 上传人根据批注检查原始资料并勘误
7. 重新上传整改后的 PDF 文件
8. 系统生成新版本 v2
9. 任务状态变为“已重新上传 / 待复审”
10. 文件状态变为“待复审”
```

### 6.3 审核人复审流程

```text
1. 审核人收到待复审提示
2. 打开新版本文件 v2
3. 系统可显示旧版本批注，也可切换查看新旧版本
4. 审核人核对问题是否修复
5. 如果通过：
   - 文件版本状态变为 approved
   - 整改任务状态变为 closed
   - 批注状态变为 resolved / closed
   - 资料项审核状态变为已通过
6. 如果不通过：
   - 审核人可继续新增批注
   - 原任务状态变为 returned 或新建二次整改任务
   - 上传人继续整改
```

## 7. 页面设计建议

### 7.1 当前列表页改造

在现有文件卡片上增加两个入口：

```text
查看
审阅
批注数
任务状态
```

示例：

```text
010001-知情同意书-v1.pdf
版本：v1
状态：已退回
批注：3条
任务：待整改
[查看] [审阅] [任务记录]
```

### 7.2 PDF 审阅页

页面布局：

```text
顶部工具栏：返回 / 文件名 / 版本 / 审核状态 / 保存 / 生成任务
左侧栏：页码缩略图
中间区：PDF预览 + 批注层
右侧栏：批注列表 + 任务单信息
```

工具按钮：

```text
选择
画框批注
缩放
上一页
下一页
显示/隐藏批注
生成整改任务
审核通过
退回整改
```

### 7.3 批注卡片样式

右侧批注列表可以这样展示：

```text
#1 第3页｜签名缺失｜严重
问题说明：受试者签名处为空，请补充完整签名页。
状态：待处理
创建人：系统管理员
时间：2026/5/12 10:25
[定位] [编辑] [删除]
```

### 7.4 任务单页面

任务单页面建议包含：

```text
任务编号
所属项目
所属阶段
资料项
文件版本
反馈人
接收人
问题数量
任务状态
批注列表
上传整改文件入口
处理记录
复审结论
```

## 8. 接口设计草案

### 8.1 获取 PDF 审阅信息

```http
GET /api/pdf-review/files/{file_version_id}
```

返回：

```json
{
  "file_version_id": 101,
  "file_name": "010001-知情同意书-v1.pdf",
  "preview_url": "/files/preview/101",
  "version_no": 1,
  "status": "reviewing",
  "annotations": []
}
```

### 8.2 获取批注列表

```http
GET /api/pdf-review/files/{file_version_id}/annotations
```

### 8.3 新增批注

```http
POST /api/pdf-review/annotations
```

请求：

```json
{
  "clinical_file_id": 10,
  "file_version_id": 101,
  "page_no": 3,
  "x": 0.235,
  "y": 0.412,
  "width": 0.188,
  "height": 0.073,
  "issue_type": "missing_signature",
  "severity": "high",
  "comment": "受试者签名缺失，请补充签名页。"
}
```

### 8.4 修改批注

```http
PATCH /api/pdf-review/annotations/{annotation_id}
```

### 8.5 删除批注

```http
DELETE /api/pdf-review/annotations/{annotation_id}
```

### 8.6 根据批注生成整改任务

```http
POST /api/correction-tasks
```

请求：

```json
{
  "clinical_file_id": 10,
  "file_version_id": 101,
  "annotation_ids": [1, 2, 3],
  "assigned_to": 25,
  "title": "知情同意书资料需整改",
  "description": "请根据批注补充签名页并重新上传。",
  "due_date": "2026-05-20"
}
```

### 8.7 上传整改文件

```http
POST /api/correction-tasks/{task_id}/submit
```

表单：

```text
file: 新PDF文件
remark: 整改说明
```

### 8.8 审核通过

```http
POST /api/correction-tasks/{task_id}/approve
```

### 8.9 再次退回

```http
POST /api/correction-tasks/{task_id}/return
```

## 9. 权限设计

建议至少区分三类角色。

### 9.1 系统管理员 / 项目管理员

权限：

```text
查看所有项目资料
上传资料
审阅资料
创建批注
生成整改任务
分派任务
复审通过
查看操作日志
```

### 9.2 审核人

权限：

```text
查看分配给自己的项目资料
打开 PDF 审阅页
新增、编辑、删除自己创建的批注
生成整改任务
复审整改文件
审核通过或再次退回
```

### 9.3 上传人

权限：

```text
查看自己上传的资料
查看分配给自己的整改任务
查看批注内容
重新上传整改文件
填写整改说明
查看复审结果
```

## 10. 文件存储设计

推荐目录结构：

```text
storage/
├── original/
│   └── project_{id}/stage_{id}/data_item_{id}/original.pdf
├── split/
│   └── project_{id}/stage_{id}/data_item_{id}/v1_split.pdf
├── review_copy/
│   └── project_{id}/stage_{id}/data_item_{id}/v1_review.pdf
├── corrected/
│   └── project_{id}/stage_{id}/data_item_{id}/v2_corrected.pdf
└── export/
    └── annotated_pdf/
```

文件命名建议：

```text
{项目编号}_{阶段编码}_{资料项编码}_{资料名称}_v{版本号}_{时间戳}.pdf
```

示例：

```text
C200_SCREENING_ICF_知情同意书_v1_20260512.pdf
C200_SCREENING_ICF_知情同意书_v2_20260515.pdf
```

## 11. 分阶段实施计划

## P0：现状梳理与数据模型对齐

周期：1-2 天

目标：明确现有系统中“文件、项目、阶段、资料项、上传人、审核状态”的表结构和接口。

任务：

1. 梳理当前文件上传和拆分后的保存路径。
2. 梳理当前文件表、资料项表、用户表、审核状态字段。
3. 确认当前 PDF 预览方式，如果没有预览，则新增预览接口。
4. 确认用户角色和权限边界。
5. 确认重新上传是否已有版本逻辑，如果没有，则新增版本表。

交付物：

```text
现有数据表关系图
新增表字段确认稿
文件存储路径确认稿
接口改造清单
```

## P1：PDF 在线预览页

周期：2-4 天

目标：实现拆分 PDF 在页面内打开预览。

任务：

1. 集成 PDF.js。
2. 新增 PDF 审阅页面路由。
3. 后端提供 PDF 文件流或预览 URL。
4. 前端实现翻页、缩放、适应宽度。
5. 页面展示文件名、版本号、资料项、审核状态。

验收标准：

```text
用户点击文件后可进入 PDF 审阅页
PDF 能够正常显示
支持多页翻页
支持缩放
不同文件均可正常打开
```

## P2：画框批注 MVP

周期：4-6 天

目标：实现画框、填写注释、保存、回显。

任务：

1. 在 PDF 渲染层上方增加透明批注层。
2. 实现鼠标拖拽画矩形框。
3. 实现批注弹窗。
4. 保存批注到后端。
5. 后端新增 pdf_annotations 表和接口。
6. 页面重新打开时加载并还原批注。
7. 右侧显示批注列表。
8. 点击批注列表定位到对应页码。

验收标准：

```text
审核人可以在 PDF 上画框
可以填写批注说明
批注保存后不丢失
刷新页面后批注可以恢复
缩放后批注位置不偏移
多页批注能够正常显示
```

## P3：整改任务单闭环

周期：4-6 天

目标：批注可以生成任务单，上传人可以查看并重新上传。

任务：

1. 新增 correction_tasks 表。
2. 新增 correction_task_annotations 表。
3. 审核人可以选择多个批注生成整改任务。
4. 默认任务接收人为原上传人。
5. 上传人拥有“我的任务”页面。
6. 上传人可以查看任务详情和批注。
7. 上传人可以重新上传整改 PDF。
8. 系统生成新的文件版本。
9. 任务状态变为待复审。

验收标准：

```text
批注可以生成任务
任务能准确分配给上传人
上传人能查看批注和问题说明
上传人能重新上传文件
新文件不会覆盖旧文件
文件版本链路清晰
```

## P4：复审与状态流转

周期：3-5 天

目标：审核人可以对整改后的文件进行复审，形成通过或再次退回。

任务：

1. 审核人查看待复审任务。
2. 审核人打开新版本文件。
3. 支持查看历史批注。
4. 审核人点击“通过”关闭任务。
5. 审核人点击“再次退回”继续批注或退回。
6. 更新资料项状态、文件状态、任务状态、批注状态。
7. 记录复审意见。

验收标准：

```text
审核人能看到待复审文件
通过后任务关闭
通过后资料项状态更新为已通过
不通过时可以再次退回
历史版本和批注不丢失
```

## P5：操作日志与基础审计

周期：2-3 天

目标：关键操作可追溯。

任务：

1. 新增操作日志表。
2. 记录新增批注、修改批注、删除批注。
3. 记录生成任务、重新上传、审核通过、再次退回。
4. 在任务详情中展示处理记录。
5. 管理员可查看操作日志。

验收标准：

```text
每次关键操作都有日志
日志包含操作人、时间、动作和对象
任务详情能看到完整流转记录
```

## P6：导出与统计增强，可后置

周期：4-7 天

目标：提升管理展示能力，不作为第一阶段强制项。

可选能力：

1. 导出带批注 PDF 副本。
2. 导出整改任务单 PDF / Word。
3. 统计各资料项问题数量。
4. 统计不同问题类型分布。
5. 统计上传人整改响应时间。
6. 统计项目、中心、阶段维度的资料质量情况。

验收标准：

```text
可以导出审阅结果
可以按项目/阶段/资料项统计问题
可以展示整改闭环效率
```

## 12. 最小可行版本 MVP 范围

建议第一阶段只做以下内容：

```text
1. PDF 在线预览
2. 画框批注
3. 批注保存和回显
4. 批注生成整改任务
5. 上传人查看任务
6. 上传人重新上传
7. 审核人复审通过 / 退回
8. 文件版本记录
```

暂不做：

```text
复杂电子签名
多人实时协同批注
OCR 自动识别错误
自动比对新旧版本
复杂流程引擎
移动端深度适配
```

这些后续可以扩展，不要在第一阶段把工程拖成泥潭。

## 13. 风险点与解决方案

### 13.1 批注框位置偏移

风险：PDF 缩放、不同屏幕、不同 DPI 下批注框偏移。

解决：使用归一化坐标，不保存屏幕像素坐标。

### 13.2 文件版本混乱

风险：重新上传覆盖旧文件，导致无法追溯。

解决：所有重新上传都生成新版本，不允许覆盖原版本。

### 13.3 原始 PDF 被污染

风险：直接向 PDF 写入批注，影响原始资料真实性。

解决：原文件只读，批注单独存数据库；需要导出时生成副本。

### 13.4 任务和批注关系混乱

风险：多个批注对应一个任务，后续状态难管理。

解决：使用任务-批注关联表，任务负责流转，批注负责问题定位。

### 13.5 权限边界不清晰

风险：上传人修改审核批注，或无关人员查看敏感资料。

解决：按项目、角色、任务归属做权限控制。

### 13.6 临床资料合规风险

风险：系统日志不足，后续难以追踪谁修改、谁审核。

解决：关键动作全量记录操作日志，保留版本和审核意见。

## 14. 与当前系统的衔接方式

当前页面已有文件展示、上传、审核状态和记录入口。建议在现有基础上做小步扩展。

### 14.1 文件卡片增加字段

```text
当前版本：v1 / v2
批注数量：3
整改任务：待处理 / 待复审 / 已关闭
审阅入口：审阅
```

### 14.2 审核状态扩展

当前已有“已上传、已通过、资料齐全”等状态，可以新增：

```text
待审阅
审阅中
已退回
待整改
待复审
复审通过
再次退回
```

### 14.3 原上传按钮逻辑扩展

原来上传按钮只负责上传文件。现在需要区分：

```text
首次上传
整改重新上传
补充上传
替换上传，需要权限控制
```

对于整改任务中的上传，应进入版本链，而不是覆盖原文件。

## 15. 推荐开发顺序

优先顺序如下：

```text
第一步：文件版本表
第二步：PDF 预览页
第三步：画框批注
第四步：批注保存与回显
第五步：批注生成任务
第六步：上传人任务页
第七步：整改重新上传
第八步：复审状态流转
第九步：日志和统计
第十步：导出带批注 PDF
```

其中，真正需要优先验证技术可行性的只有两个点：

```text
PDF.js 是否能稳定预览现有拆分 PDF
Canvas / SVG 批注层是否能与 PDF 缩放保持对齐
```

这两个点验证通过后，后面的任务单和版本流转主要是常规业务系统开发。

## 16. 给领导汇报时的表述

可以这样说明：

> 技术上可以实现。我们建议采用“原始 PDF 留存不改动，审阅副本或页面批注层用于审核”的方案。审核人在页面中打开拆分后的 PDF 文件，通过画框标记问题区域并填写批注说明，系统将批注作为结构化数据保存，并支持一键生成整改任务单反馈给上传人。上传人根据批注勘误后重新上传，系统生成新版本，审核人再进行复审。整个过程保留原文件、批注、任务、版本和审核日志，既满足资料质控，也方便后续追溯和审计。

更简短版本：

> 可以做，建议作为“PDF 在线审阅与整改闭环”模块建设。原文件不修改，批注存在数据库里，页面展示时叠加到 PDF 上。审核人画框批注后生成任务单，上传人整改重传，系统保留版本，审核人复审，最终形成上传、审核、退回、整改、复审、通过的闭环。

## 17. 阶段性交付预估

在当前系统已有基础框架的前提下，建议排期如下：

```text
第1周：PDF预览 + 文件版本表 + 批注表
第2周：画框批注 + 批注保存回显 + 批注列表
第3周：整改任务单 + 上传人任务页 + 重新上传
第4周：复审流转 + 操作日志 + 页面优化 + 测试验收
```

如果只做 MVP，可压缩为 2 周左右：

```text
第1周：PDF预览 + 画框批注 + 保存回显
第2周：任务单 + 重新上传 + 复审状态
```

如果要做得更稳，建议按 4 周推进。因为临床资料系统要考虑版本、权限和审计，不能只做一个能点能画的演示页面。

## 18. 结论

该需求在技术上完全可实现，并且非常适合作为当前临床数据系统的下一阶段核心升级。

推荐定位：

```text
临床资料在线审阅与整改闭环模块
```

核心建设思路：

```text
原文件不动
副本或页面预览用于审阅
批注结构化保存
批注生成任务单
上传人整改重传
审核人复审闭环
版本和日志全程追溯
```

该模块上线后，系统将不再只是“资料上传与归档工具”，而是具备资料质量控制、问题反馈、责任分派、复审追踪能力的临床数据协作平台。

