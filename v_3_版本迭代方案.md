# V3版本迭代方案

## 0. 方案定位

本方案面向当前 `clinical-data-system` 的 PDF 资料包识别、拆分、结构化抽取与审核入库链路，目标是将现有“能识别、能切段”的 V2 能力升级为“资料项分准、字段可审、结构化可沉淀”的 V3 能力。

本方案不是展示型文档，不写领导汇报口径，直接作为开发规划、Codex 任务拆解、代码改造与验收依据使用。

---

## 1. V3 总目标

### 1.1 核心目标

V3 版本围绕四个层级推进：

1. **P0：资料项切分准确性增强**
   - 解决 PDF 资料包内不同资料项被误合并、误切分、误分类的问题。
   - 优先保证每个资料片段的页码范围和资料类型正确。

2. **P1：重点资料字段抽取与详情页展示**
   - 在 P0 分准的基础上，对少数高价值资料项做结构化字段抽取。
   - 字段结果挂在资料项下，不直接污染受试者主表。

3. **P2：业务逻辑校验与资料完整性检查**
   - 对已抽取字段进行时间顺序、资料完整性、字段一致性等校验。
   - 输出可人工确认的异常提示。

4. **P3：审核确认后的 JSON 资产入库与研发复用**
   - 审核后的结构化结果以 JSON 形式沉淀。
   - 支持后续临床、研发、算法、数据治理侧复用。

### 1.2 V3 不做的事情

V3 初期不追求以下能力：

- 不追求手写签名文字内容精准识别。
- 不把所有资料项一次性字段化。
- 不直接用 OCR 结果覆盖正式业务字段。
- 不把未审核字段写入受试者主信息作为正式数据。
- 不做医生级医学判断，只做资料结构化、文本归类和疑似风险提示。
- 不优先做复杂 AI 大模型抽取链路，先完成规则 + OCR + 人工审核闭环。

---

## 2. 当前问题清单

### 2.1 资料项切分问题

当前资料包识别存在以下问题：

- “知情同意书 1-5 页”实际只有第 1 页是知情同意书。
- 第 2 页为“知情同意书交接记录表”，但被误合并。
- 第 3 页为“医学影像科检查报告单”，但被误合并。
- 第 4-12 页为“门诊病历”，但被误合并。
- 说明当前切分逻辑对“新资料项起始页”的识别不足。

### 2.2 文档类型识别问题

当前识别更偏“关键词包含”，容易出现以下误判：

- 短标题覆盖长标题。
- “知情同意书交接记录表”被归为“知情同意书”。
- 医院检查报告、门诊病历、临床表单之间区分不足。
- 页面标题缺失时，无法基于结构特征判断类型。

### 2.3 前端人工校正能力不足

当前页面虽然可以展示识别片段，但还需要增强：

- 片段拆分。
- 片段合并。
- 页码范围修改。
- 资料项类型修改。
- 人工确认状态保存。
- 人工结果优先于重新识别结果。

### 2.4 字段抽取链路尚未闭环

当前系统已有 OCR 文本和资料项片段，但尚未形成完整字段链路：

- 缺少按资料项类型配置字段模板。
- 缺少字段抽取结果表或 JSON 存储结构。
- 缺少字段级审核状态。
- 缺少详情页字段展示与修改入口。
- 缺少审核后 JSON 输出。

---

## 3. 总体架构调整

### 3.1 V3 链路总览

```text
PDF 上传
  ↓
文本层读取 / OCR 识别
  ↓
逐页文本标准化
  ↓
P0：页面级资料类型识别
  ↓
P0：资料项边界切分
  ↓
P0：人工拆分 / 合并 / 修正 / 确认
  ↓
P1：按资料项类型做字段抽取
  ↓
P1：字段展示 / 人工修改 / 审核
  ↓
P2：时间、完整性、一致性校验
  ↓
P3：审核确认 JSON 入库 / 导出 / 研发复用
```

### 3.2 后端核心模块建议

建议新增或拆分以下模块：

```text
backend/app/services/pdf_packet_analyzer/
  page_text_normalizer.py        # 页面 OCR 文本清洗与标准化
  document_type_registry.py      # 资料项类型注册表
  page_classifier.py             # 页面级资料类型识别
  segment_builder.py             # 页到片段的切分逻辑
  segment_confidence.py          # 片段置信度计算
  extraction_templates.py        # 字段抽取模板
  field_extractors.py            # 字段抽取器
  validation_rules.py            # P2 逻辑校验规则
  json_asset_builder.py          # 审核后 JSON 资产生成
```

当前如果不想大规模改目录，也可以先在 `pdf_packets.py` 内部拆函数，但 V3 建议逐步模块化，避免后续继续堆成巨石函数。

### 3.3 数据状态总览

V3 中建议区分三类状态：

| 层级 | 状态字段 | 说明 |
|---|---|---|
| 资料包 | packet_status | 未识别、已识别、部分确认、全部确认 |
| 资料片段 | segment_status | 自动识别、待确认、人工已确认、人工修改过 |
| 字段 | review_status | extracted、reviewed、confirmed、rejected |

---

# P0：资料项切分准确性增强

## P0 目标

P0 的唯一核心目标：**把资料项页码范围和资料类型分准**。

P0 不做复杂字段抽取，只允许做“用于分类的轻量特征识别”。

---

## P0.1 标题强识别

### 目标

基于每页 OCR 文本，优先识别页面标题或显著标题，判断该页是否为新资料项起始页。

### 实现内容

建立资料项标题字典，按优先级匹配。

示例：

```python
DOCUMENT_TITLE_RULES = [
    {
        "doc_type": "informed_consent_handover",
        "name": "知情同意书交接记录表",
        "title_patterns": ["知情同意书交接记录表", "知情同意书交接表", "知情同意交接记录"],
        "priority": 100,
    },
    {
        "doc_type": "informed_consent",
        "name": "知情同意书",
        "title_patterns": ["知情同意书", "受试者知情同意书"],
        "priority": 80,
    },
    {
        "doc_type": "imaging_report",
        "name": "医学影像检查报告单",
        "title_patterns": ["医学影像科检查报告单", "影像检查报告单", "放射科检查报告"],
        "priority": 90,
    },
    {
        "doc_type": "outpatient_record",
        "name": "门诊病历",
        "title_patterns": ["门诊病历", "门诊电子病历", "门急诊病历"],
        "priority": 85,
    },
]
```

### 关键规则

1. **长标题优先**
   - “知情同意书交接记录表”优先级高于“知情同意书”。

2. **专有标题优先**
   - “医学影像科检查报告单”优先级高于泛化的“检查报告”。

3. **标题位置优先**
   - OCR 文本前 20 行命中标题，置信度高。
   - 全文中部命中标题，置信度降低。

4. **标题命中后可强制切段**
   - 如果当前页命中高置信度新标题，应强制开启新 segment。

### 验收标准

- 第 1 页识别为“知情同意书”。
- 第 2 页识别为“知情同意书交接记录表”。
- 第 3 页识别为“医学影像检查报告单”。
- 第 4 页识别为“门诊病历”。
- 不允许“知情同意书交接记录表”被归为“知情同意书”。

---

## P0.2 页面文本标准化

### 目标

在标题识别和特征判断前，对 OCR 文本做统一清洗，减少 OCR 噪声影响。

### 实现内容

新增 `page_text_normalizer.py`。

处理内容：

- 全角转半角。
- 中文冒号、英文冒号统一。
- 多空格合并。
- 多换行合并。
- 页眉页脚常见噪声移除。
- 扫描水印、二维码提示文本弱化。
- 数字日期格式标准化。
- OCR 常见错字映射。

示例：

```python
def normalize_page_text(text: str) -> NormalizedPageText:
    return NormalizedPageText(
        raw_text=text,
        normalized_text=normalized,
        lines=lines,
        head_lines=lines[:20],
        tail_lines=lines[-10:],
    )
```

### 输出结构

```python
@dataclass
class NormalizedPageText:
    raw_text: str
    normalized_text: str
    lines: list[str]
    head_lines: list[str]
    tail_lines: list[str]
```

### 验收标准

- 所有页面分类逻辑统一使用标准化后的文本。
- 原始 OCR 文本仍保留，不被覆盖。
- 标准化结果可在 debug 日志中查看。

---

## P0.3 文档类型注册表

### 目标

将资料项类型、标题规则、结构特征、字段模板、优先级统一配置，避免硬编码散落在多个函数中。

### 建议结构

```python
@dataclass
class DocumentTypeRule:
    doc_type: str
    display_name: str
    title_patterns: list[str]
    feature_patterns: list[str]
    negative_patterns: list[str]
    priority: int
    allow_multi_page: bool
    likely_continuation_patterns: list[str]
```

### 初始资料类型

P0 至少覆盖：

| doc_type | 显示名 | 多页 |
|---|---|---|
| informed_consent | 知情同意书 | 可能 |
| informed_consent_handover | 知情同意书交接记录表 | 通常单页 |
| imaging_report | 医学影像检查报告单 | 通常单页 |
| outpatient_record | 门诊病历 | 可能多页 |
| vital_sign_record | 生命体征记录表 | 通常单页 |
| comfort_evaluation | 舒适度评价表 | 通常单页 |
| image_quality_evaluation | 图像质量评价表 | 可能多页 |
| bowel_preparation | 肠道准备情况 | 可能多页 |
| unknown | 未识别资料 | 可多页 |

### 验收标准

- 新增资料项类型时，只需要改注册表，不需要改核心切分逻辑。
- 前端资料项下拉选项与后端注册表保持一致。

---

## P0.4 结构特征识别

### 目标

标题缺失或 OCR 标题识别失败时，通过页面内容结构判断资料类型。

### 示例规则

#### 知情同意书

特征词：

- 项目名称
- 申办方
- 研究者
- 受试者
- 知情同意
- 签署日期
- 签名

#### 知情同意书交接记录表

特征词：

- 交接记录
- 领取知情同意书时间
- 交回知情同意书时间
- 领取人
- 交回人
- 受试者签名

#### 医学影像检查报告单

特征词：

- 检查号
- 检查项目
- 影像所见
- 诊断意见
- 报告医生
- 审核医生
- 医学影像科

#### 门诊病历

特征词：

- 主诉
- 现病史
- 既往史
- 体格检查
- 辅助检查
- 初步诊断
- 处理意见
- 医师签名

### 打分方式

```text
标题命中：+60 到 +100
强结构词命中：+10 到 +20
普通特征词命中：+3 到 +8
负向词命中：-20 到 -80
延续上一页特征：+10 到 +30
```

### 输出结构

```python
@dataclass
class PageClassificationResult:
    page_no: int
    doc_type: str
    display_name: str
    confidence: float
    matched_title: str | None
    matched_features: list[str]
    negative_hits: list[str]
    is_strong_start: bool
    reason: str
```

### 验收标准

- 标题缺失但存在“主诉/现病史/既往史/诊断”的页面可识别为门诊病历延续页。
- 检查报告不会被误识别为门诊病历。
- 交接记录不会被误识别为知情同意书。

---

## P0.5 负向规则与互斥规则

### 目标

解决资料项之间关键词重叠导致的误判。

### 实现内容

为每类资料增加 `negative_patterns` 和互斥规则。

示例：

```python
INFORMED_CONSENT_NEGATIVE = [
    "交接记录表",
    "检查报告单",
    "影像所见",
    "诊断意见",
    "门诊病历",
    "主诉",
    "现病史",
]
```

如果页面同时命中：

- “知情同意书”
- “交接记录表”

则归为：

```text
知情同意书交接记录表
```

如果页面同时命中：

- “检查号”
- “影像所见”
- “诊断意见”

则归为：

```text
医学影像检查报告单
```

即使页面存在患者姓名、性别、年龄，也不能归为门诊病历。

### 验收标准

- 第 2 页交接表不被归到知情同意书。
- 第 3 页检查报告不被归到门诊病历。
- 带有患者基本信息的报告单不被误归类。

---

## P0.6 连续页切分逻辑

### 目标

将页面级分类结果合并为资料片段，正确处理多页资料延续和新资料项起始。

### 切分规则

1. 第一页开启第一个 segment。
2. 当前页命中强标题，且 doc_type 与上一页不同，开启新 segment。
3. 当前页命中强标题，即使 doc_type 与上一页相同，也根据资料类型判断是否开启新 segment。
4. 当前页无强标题，但分类结果与上一页一致，则延续上一 segment。
5. 当前页无强标题，分类低置信度，但上一资料项允许多页，且存在延续特征，则延续上一 segment。
6. 当前页分类低置信度，且无延续依据，则开启 unknown segment 或标记待确认。

### 多页策略

| 资料类型 | 默认策略 |
|---|---|
| 知情同意书 | 可多页，但遇到交接表/报告/病历强制切断 |
| 交接记录表 | 默认单页 |
| 影像报告 | 默认单页，除非下一页明确为报告续页 |
| 门诊病历 | 可多页，连续主诉/诊断/医嘱等特征延续 |
| 图像质量评价表 | 可多页 |
| 肠道准备情况 | 可多页 |

### 验收标准

- 010005.pdf 的前 12 页应至少切成：
  - 1-1 知情同意书
  - 2-2 知情同意书交接记录表
  - 3-3 医学影像检查报告单
  - 4-12 门诊病历

---

## P0.7 置信度与待确认机制

### 目标

避免系统在低置信度场景下强行归类。

### 置信度建议

```text
>= 0.85：高置信度，可自动识别
0.60 - 0.85：中置信度，标记待确认
< 0.60：低置信度，标记未识别或待人工处理
```

### segment 状态

```text
auto_confirmed_candidate  # 系统高置信候选
pending_review            # 待人工确认
manually_confirmed        # 人工确认
manually_modified         # 人工修改
unknown                   # 未识别
```

### 前端展示

- 高置信度：显示“待确认”，但可批量确认。
- 中低置信度：显示“需核对”。
- unknown：显示“未识别资料”，强提示人工选择资料项。

### 验收标准

- 低置信度资料不应伪装成准确结果。
- 页面必须能看到置信度来源或识别原因。

---

## P0.8 前端片段拆分能力

### 目标

用户可以将错误合并的片段拆成多个资料项。

### 示例

原结果：

```text
1-5 知情同意书
```

用户拆分为：

```text
1-1 知情同意书
2-2 知情同意书交接记录表
3-3 医学影像检查报告单
4-5 门诊病历
```

### 前端操作建议

每个 segment 行增加：

- 拆分按钮
- 合并按钮
- 保存按钮
- 确认按钮
- 删除按钮
- 预览按钮

拆分弹窗字段：

| 字段 | 类型 |
|---|---|
| 原片段页码 | 只读 |
| 拆分点 | 输入页码 |
| 前半段资料项 | 下拉选择 |
| 后半段资料项 | 下拉选择 |
| 是否立即确认 | checkbox |

### 后端 API 建议

```text
POST /api/v1/pdf-packets/{packet_id}/segments/{segment_id}/split
```

请求体：

```json
{
  "splits": [
    {"start_page": 1, "end_page": 1, "document_item_id": 1},
    {"start_page": 2, "end_page": 2, "document_item_id": 2},
    {"start_page": 3, "end_page": 3, "document_item_id": 3}
  ]
}
```

### 验收标准

- 可以把一个片段拆成两个或多个片段。
- 拆分后页码不能重叠。
- 拆分后页码不能超出原片段范围。
- 拆分后原片段被替换或标记失效。

---

## P0.9 前端片段合并能力

### 目标

用户可以将被错误拆开的连续片段合并为一个资料项。

### 合并规则

- 只能合并同一资料包内片段。
- 默认只允许合并页码连续的片段。
- 合并后需要选择一个最终资料项类型。
- 合并后保留原片段操作记录。

### 后端 API 建议

```text
POST /api/v1/pdf-packets/{packet_id}/segments/merge
```

请求体：

```json
{
  "segment_ids": [12, 13, 14],
  "document_item_id": 8,
  "status": "manually_modified"
}
```

### 验收标准

- 可将连续多个片段合并。
- 合并后生成新 segment 或更新第一个 segment。
- 被合并片段不再作为有效片段展示。

---

## P0.10 页码范围和资料项类型手动修改

### 目标

允许用户直接修改 segment 页码范围和资料项类型。

### 字段

| 字段 | 说明 |
|---|---|
| start_page | 起始页 |
| end_page | 结束页 |
| document_item_id | 资料项类型 |
| segment_status | 修改后状态 |
| manual_note | 人工备注 |

### 后端 API 建议

```text
PATCH /api/v1/pdf-packets/{packet_id}/segments/{segment_id}
```

### 验收标准

- 修改页码后校验不能与其他有效片段冲突。
- 修改资料项后状态变为 `manually_modified`。
- 修改记录写入日志。

---

## P0.11 人工确认与识别结果冻结

### 目标

人工确认后的资料项结果不能被重新识别覆盖。

### 实现规则

- segment 增加 `locked_by_user` 或 `is_manual_locked`。
- 重新识别时默认跳过人工锁定片段。
- 如需覆盖，必须显式传入 `force=true`。
- 保留历史版本。

### 后端字段建议

```text
review_status
reviewed_by
reviewed_at
is_manual_locked
revision
```

### 验收标准

- 人工确认后再次点击“重新识别”，确认结果不丢失。
- 支持“解除确认/重新识别”。

---

## P0.12 调试报告与识别日志

### 目标

每次识别生成可排查的 debug 信息，便于 Codex 和开发者定位误判原因。

### 日志内容

每页输出：

```json
{
  "page_no": 2,
  "predicted_doc_type": "informed_consent_handover",
  "confidence": 0.93,
  "matched_title": "知情同意书交接记录表",
  "matched_features": ["领取知情同意书时间", "交回知情同意书时间"],
  "negative_hits": [],
  "reason": "strong title matched"
}
```

每个 segment 输出：

```json
{
  "segment_id": 1,
  "page_range": [1, 1],
  "doc_type": "informed_consent",
  "confidence": 0.91,
  "build_reason": "new strong title on page 1"
}
```

### 验收标准

- 后端保留最近一次识别报告。
- 前端可选展示“识别原因”。
- 本地开发可导出 JSON 调试文件。

---

# P1：重点资料字段抽取与详情页展示

## P1 目标

在资料项切分准确的前提下，对重点资料项进行字段抽取。字段抽取结果只作为预填信息，需要人工审核确认。

---

## P1.1 字段模板体系

### 目标

按资料项类型定义字段模板。

### 字段模板结构

```python
@dataclass
class FieldTemplate:
    field_key: str
    field_name: str
    field_type: str
    required: bool
    extraction_patterns: list[str]
    normalizer: str | None
    review_required: bool
```

### 字段结果结构

```json
{
  "field_key": "subject_signed_at",
  "field_name": "受试者签署日期",
  "value": "2023-03-17",
  "raw_value": "2023.3.17",
  "source": "ocr",
  "confidence": 0.81,
  "review_status": "extracted",
  "updated_by": null,
  "updated_at": null
}
```

### 验收标准

- 新增字段不需要改数据库表结构。
- 字段模板可按资料项类型扩展。

---

## P1.2 知情同意书字段抽取

### 字段清单

| field_key | 字段名 | 类型 | P1 要求 |
|---|---|---|---|
| project_name | 项目名称 | text | 抽取 |
| sponsor | 申办方 | text | 抽取 |
| center_name | 中心/研究中心 | text | 抽取 |
| department | 科室 | text | 可选 |
| investigator_name | 主要研究者 | text | 可选 |
| subject_code | 受试者编号 | text | 抽取 |
| subject_gender | 性别 | enum | 抽取 |
| consent_received_at | 领取知情同意书时间 | date | 可选 |
| subject_signed_at | 受试者签署日期 | date | 抽取 |
| investigator_signed_at | 研究者签署日期 | date | 抽取 |
| subject_signature_present | 受试者签字是否存在 | boolean/uncertain | 重点 |
| investigator_signature_present | 研究者签字是否存在 | boolean/uncertain | 重点 |

### 签名处理规则

P1 不识别签名文字内容，只判断签名区域是否存在笔迹。

可先做低成本判断：

- OCR 文本中签名字段后是否有非空内容。
- 页面底部签名区域附近是否存在较多深色像素。
- 如果没有图像级能力，先允许人工勾选。

输出值：

```text
true       # 疑似有签名
false      # 疑似无签名
uncertain  # 无法判断
```

### 验收标准

- 能抽出知情同意相关日期。
- 能展示签字存在性字段。
- 签字存在性必须允许人工修改。

---

## P1.3 知情同意书交接记录表字段抽取

### 字段清单

| field_key | 字段名 | 类型 |
|---|---|---|
| subject_code | 受试者编号 | text |
| consent_received_at | 受试者领取知情同意书时间 | datetime/date |
| consent_returned_at | 受试者交回知情同意书时间 | datetime/date |
| receiver_signature_present | 领取人签字是否存在 | boolean/uncertain |
| returner_signature_present | 交回人签字是否存在 | boolean/uncertain |

### 验收标准

- 第 2 页被正确识别后，可使用该模板抽取。
- 日期字段可被标准化。

---

## P1.4 医学影像检查报告单字段抽取

### 字段清单

| field_key | 字段名 | 类型 |
|---|---|---|
| hospital_name | 医院名称 | text |
| patient_name | 姓名 | text |
| gender | 性别 | enum |
| age | 年龄 | number/text |
| exam_no | 检查号 | text |
| registration_no | 登记号 | text |
| exam_item | 检查项目 | text |
| exam_part | 检查部位 | text |
| exam_time | 检查时间 | datetime/date |
| report_time | 报告时间 | datetime/date |
| imaging_findings | 影像所见 | long_text |
| diagnosis_opinion | 诊断意见 | long_text |
| report_doctor | 报告医生 | text |
| audit_doctor | 审核医生 | text |

### 抽取方式

- 优先基于关键词区块切分。
- `影像所见` 和 `诊断意见` 作为长文本字段处理。
- 医院名称可从页面标题附近抽取。

### 验收标准

- 至少可抽出姓名、性别、年龄、检查号、影像所见、诊断意见。
- 长文本字段允许人工编辑。

---

## P1.5 门诊病历字段抽取

### 字段清单

| field_key | 字段名 | 类型 |
|---|---|---|
| visit_date | 就诊日期 | date |
| department | 科室 | text |
| chief_complaint | 主诉 | long_text |
| present_illness | 现病史 | long_text |
| past_history | 既往史 | long_text |
| allergy_history | 过敏史 | long_text |
| physical_exam | 体格检查 | long_text |
| auxiliary_exam | 辅助检查 | long_text |
| diagnosis | 诊断 | long_text |
| treatment_plan | 处理意见 | long_text |
| doctor_name | 医生姓名 | text |
| doctor_signature_present | 医生签名是否存在 | boolean/uncertain |

### 抽取方式

- 基于字段标题切块。
- 支持跨页合并文本。
- 如果某字段标题不存在，则留空，不强行猜测。

### 验收标准

- 多页门诊病历可合并 OCR 文本后抽取字段。
- 主诉、现病史、既往史、诊断、处理意见至少能识别部分字段。
- 字段缺失时显示为空，不乱填。

---

## P1.6 详情页字段展示

### 目标

在受试者详情页中，每个资料项可展开查看字段抽取结果。

### 前端交互

每个资料项行支持：

- 展开字段。
- 编辑字段。
- 保存字段。
- 审核字段。
- 确认字段。
- 查看原文 OCR。
- 查看 PDF 原页。

### 字段展示列

| 字段名 | 自动抽取值 | 修正值 | 置信度 | 状态 | 操作 |
|---|---|---|---|---|---|

### 状态设计

```text
extracted   # 系统抽取
reviewed    # 人工看过
confirmed   # 人工确认
rejected    # 人工否定
empty        # 未抽取到
```

### 验收标准

- 每个资料项下可查看字段。
- 字段可修改。
- 修改后状态变化。
- 保存后刷新不丢失。

---

## P1.7 字段结果存储

### 目标

字段结果挂在资料片段下，避免污染主表。

### 数据库存储方案 A：JSON 字段

在 segment 表增加：

```text
extracted_fields JSON
confirmed_fields JSON
```

优点：

- 实现快。
- 字段灵活。
- 适合字段清单未稳定阶段。

缺点：

- 后续复杂查询不方便。

### 数据库存储方案 B：字段结果表

新增表：

```text
pdf_packet_segment_fields
```

字段：

```text
id
segment_id
field_key
field_name
field_type
raw_value
value
corrected_value
source
confidence
review_status
reviewed_by
reviewed_at
created_at
updated_at
```

优点：

- 字段级审核清晰。
- 查询统计方便。

缺点：

- 开发量略大。

### 建议

V3 初期可以采用 B 方案，字段级审核和后续统计更清楚。若时间紧，可先用 JSON 快速闭环，后续迁移字段表。

---

# P2：业务逻辑校验与资料完整性检查

## P2 目标

基于 P1 已抽取并审核的字段，进行业务规则校验，输出异常提示和完整性结果。

---

## P2.1 时间顺序校验

### 规则示例

| 规则 | 异常条件 |
|---|---|
| 知情同意时间 ≤ 检查时间 | 知情同意晚于检查 |
| 检查时间 ≤ 报告时间 | 报告早于检查 |
| 领取知情同意书时间 ≤ 签署时间 | 签署早于领取 |
| 签署时间 ≤ 交回时间 | 交回早于签署 |
| 入组时间 ≥ 知情同意时间 | 入组早于知情同意 |

### 输出结构

```json
{
  "rule_key": "consent_before_exam",
  "level": "warning",
  "message": "知情同意时间晚于检查时间，请核对。",
  "related_fields": ["subject_signed_at", "exam_time"],
  "status": "pending_review"
}
```

### 验收标准

- 可在详情页显示时间异常提醒。
- 异常可人工标记为已核对。

---

## P2.2 资料完整性校验

### 目标

按项目、中心、受试者阶段检查资料是否齐全。

### 初始规则

每个受试者建议检查：

- 知情同意书
- 知情同意书交接记录表
- 检查报告/影像报告
- 门诊病历
- 生命体征记录表
- 舒适度评价表
- 图像质量评价表
- 肠道准备情况

### 状态

```text
complete      # 齐全
missing       # 缺失
pending       # 存在但未确认
not_required  # 当前阶段不要求
```

### 验收标准

- 详情页显示资料完整性列表。
- 缺失项可一眼看到。
- 未确认项区别于缺失项。

---

## P2.3 字段一致性校验

### 示例

- 受试者编号在不同资料中应一致。
- 姓名在检查报告、门诊病历中应一致。
- 性别在多个资料中应一致。
- 年龄差异应合理。
- 中心名称应与筛选条件一致。

### 输出

```json
{
  "rule_key": "subject_code_consistency",
  "level": "error",
  "message": "不同资料中的受试者编号不一致。",
  "values": [
    {"segment_id": 1, "value": "010005"},
    {"segment_id": 3, "value": "01005"}
  ]
}
```

### 验收标准

- 同一字段多来源冲突时提示。
- 允许人工选择最终确认值。

---

## P2.4 病历关键信息进一步结构化

### 目标

在门诊病历字段基础上，提取更细的临床信息标签。

### 初始标签

- 是否提及消化道症状
- 是否提及腹痛
- 是否提及腹泻
- 是否提及便血
- 是否提及既往胃肠镜检查
- 是否提及用药史
- 是否提及过敏史
- 是否提及禁忌症风险

### 输出方式

必须使用“提及/疑似/未提及/无法判断”，不做诊断结论。

```json
{
  "mentioned_gastrointestinal_symptom": {
    "value": "mentioned",
    "evidence": "主诉中出现腹痛相关描述",
    "review_status": "extracted"
  }
}
```

### 验收标准

- 不输出确定性医学诊断。
- 每个标签保留证据文本。
- 标签允许人工修改。

---

# P3：审核确认 JSON 资产入库

## P3 目标

将人工审核确认后的资料项、字段、校验结果沉淀为结构化 JSON，供系统内部、研发侧、算法侧复用。

---

## P3.1 JSON 资产结构设计

### 受试者级 JSON

```json
{
  "project": "小肠项目",
  "center": "北京友谊",
  "subject_no": "010005",
  "packet_id": 1,
  "documents": [],
  "summary_fields": {},
  "validation_results": [],
  "version": "v3.0",
  "generated_at": "2026-05-13T10:00:00"
}
```

### 资料项级 JSON

```json
{
  "document_type": "informed_consent",
  "document_name": "知情同意书",
  "page_range": [1, 1],
  "segment_status": "manually_confirmed",
  "fields": {},
  "ocr_text_ref": null,
  "pdf_ref": null
}
```

### 字段级 JSON

```json
{
  "field_key": "subject_signed_at",
  "field_name": "受试者签署日期",
  "value": "2023-03-17",
  "raw_value": "2023.3.17",
  "source": "ocr",
  "confidence": 0.81,
  "review_status": "confirmed",
  "reviewed_by": "user",
  "reviewed_at": "2026-05-13T10:00:00"
}
```

---

## P3.2 JSON 生成策略

### 触发方式

- 用户点击“生成结构化 JSON”。
- 资料包全部确认后自动生成。
- 定时任务批量生成。

### 生成规则

- 只写入 `confirmed` 字段作为正式字段。
- `reviewed` 字段可作为候选字段写入候选区。
- `extracted` 未审核字段默认不进入正式区。
- 保留 OCR 原文引用，不把全部 OCR 文本塞入主 JSON。

### 验收标准

- JSON 可下载。
- JSON 可回读。
- JSON 与当前人工确认结果一致。

---

## P3.3 JSON 版本管理

### 目标

支持多次审核、多次生成，保留版本。

### 字段建议

```text
asset_version
asset_status
generated_by
generated_at
source_packet_revision
```

### 版本规则

- 每次生成新 JSON，版本号 +1。
- 可以标记某个版本为当前有效版本。
- 历史版本只读。

### 验收标准

- 不覆盖历史 JSON。
- 可查看当前有效版本。

---

## P3.4 研发侧复用接口

### API 建议

```text
GET /api/v1/subjects/{subject_id}/structured-json
GET /api/v1/pdf-packets/{packet_id}/structured-json
GET /api/v1/structured-assets?project_id=&center_id=&subject_no=
```

### 权限建议

- 临床人员：可审核、确认。
- 研发人员：默认只读已确认 JSON。
- 管理员：可导出、回滚、重新生成。

### 验收标准

- 研发侧可按受试者编号获取 JSON。
- 未确认字段不作为正式数据暴露。

---

# P4：可选增强项

P4 不进入 V3 主线闭环，作为后续优化。

## P4.1 图像级签名区域检测

### 目标

对签名区域进行图像检测，判断是否存在笔迹。

### 方式

- 基于固定区域裁剪。
- 基于表格线检测定位签名区域。
- 基于深色像素密度判断。
- 后续可接入轻量视觉模型。

---

## P4.2 OCR block 坐标增强

### 目标

从整页文本升级为带坐标块的 OCR 结果。

### 用途

- 字段定位。
- 高亮来源区域。
- 签名区域判断。
- 表格结构还原。

---

## P4.3 LLM 辅助字段抽取

### 目标

在规则抽取稳定后，引入本地模型或可控模型做辅助抽取。

### 约束

- LLM 只做候选，不直接确认为正式字段。
- 必须保留证据文本。
- 必须经过人工审核。

---

# 4. 数据库改造建议

## 4.1 pdf_packet_segments 表增强

建议字段：

```text
id
packet_id
start_page
end_page
document_item_id
doc_type
display_name
ocr_text
classification_confidence
classification_reason
matched_features JSON
segment_status
review_status
is_manual_locked
reviewed_by
reviewed_at
revision
created_at
updated_at
is_active
```

## 4.2 新增 segment_fields 表

```text
id
segment_id
field_key
field_name
field_type
raw_value
value
corrected_value
source
confidence
review_status
reviewed_by
reviewed_at
created_at
updated_at
```

## 4.3 新增 structured_assets 表

```text
id
subject_id
packet_id
asset_type
asset_version
asset_status
json_content
created_by
created_at
is_current
```

## 4.4 新增 segment_operation_logs 表

```text
id
packet_id
segment_id
operation_type
before_data JSON
after_data JSON
operator
created_at
```

操作类型：

```text
split
merge
update_range
update_type
confirm
unlock
reanalyze
generate_json
```

---

# 5. API 改造清单

## 5.1 P0 API

```text
POST   /api/v1/pdf-packets/{packet_id}/reanalyze
GET    /api/v1/pdf-packets/{packet_id}/segments
PATCH  /api/v1/pdf-packets/{packet_id}/segments/{segment_id}
POST   /api/v1/pdf-packets/{packet_id}/segments/{segment_id}/split
POST   /api/v1/pdf-packets/{packet_id}/segments/merge
POST   /api/v1/pdf-packets/{packet_id}/segments/{segment_id}/confirm
POST   /api/v1/pdf-packets/{packet_id}/segments/{segment_id}/unlock
GET    /api/v1/pdf-packets/{packet_id}/analysis-report
```

## 5.2 P1 API

```text
POST   /api/v1/pdf-packets/{packet_id}/segments/{segment_id}/extract-fields
GET    /api/v1/pdf-packets/{packet_id}/segments/{segment_id}/fields
PATCH  /api/v1/pdf-packets/{packet_id}/segments/{segment_id}/fields/{field_id}
POST   /api/v1/pdf-packets/{packet_id}/segments/{segment_id}/fields/{field_id}/confirm
POST   /api/v1/pdf-packets/{packet_id}/segments/{segment_id}/fields/batch-confirm
```

## 5.3 P2 API

```text
POST   /api/v1/subjects/{subject_id}/validate-structured-data
GET    /api/v1/subjects/{subject_id}/validation-results
POST   /api/v1/subjects/{subject_id}/validation-results/{result_id}/review
```

## 5.4 P3 API

```text
POST   /api/v1/subjects/{subject_id}/structured-assets/generate
GET    /api/v1/subjects/{subject_id}/structured-assets/current
GET    /api/v1/subjects/{subject_id}/structured-assets/{asset_id}
GET    /api/v1/pdf-packets/{packet_id}/structured-json
```

---

# 6. 前端改造清单

## 6.1 PDF资料包页面

新增或增强：

- 资料片段列表。
- 每个片段显示页码、资料项、置信度、状态、操作。
- 片段拆分弹窗。
- 片段合并操作。
- 页码范围编辑。
- 资料项类型下拉。
- 人工确认按钮。
- 识别原因展开。
- 调试报告查看。

## 6.2 PDF 预览联动

增强：

- 点击 segment 跳转对应页。
- 点击字段跳转来源页。
- 后续支持高亮来源区域。

## 6.3 受试者详情页

新增：

- 资料完整性面板。
- 资料项字段展开区。
- 字段编辑与确认。
- 结构化 JSON 查看/下载。
- 校验异常提示。

## 6.4 状态视觉设计

建议：

| 状态 | 展示 |
|---|---|
| 高置信自动识别 | 蓝色/普通 |
| 待确认 | 黄色 |
| 人工已确认 | 绿色 |
| 人工修改 | 紫色/蓝紫 |
| 异常 | 红色 |
| 未识别 | 灰色 |

---

# 7. 测试计划

## 7.1 P0 测试用例

### 010005.pdf 前 12 页

预期：

```text
1-1 知情同意书
2-2 知情同意书交接记录表
3-3 医学影像检查报告单
4-12 门诊病历
```

### 长标题优先测试

输入页面同时包含：

```text
知情同意书
交接记录表
```

预期：

```text
知情同意书交接记录表
```

### 检查报告与门诊病历区分测试

输入页面包含：

```text
检查号
影像所见
诊断意见
```

预期：

```text
医学影像检查报告单
```

输入页面包含：

```text
主诉
现病史
既往史
诊断
处理意见
```

预期：

```text
门诊病历
```

## 7.2 P1 测试用例

### 知情同意书

预期能抽：

- 项目名称
- 申办方
- 中心
- 受试者编号
- 日期
- 签字是否存在

### 影像报告

预期能抽：

- 姓名
- 性别
- 年龄
- 检查号
- 影像所见
- 诊断意见

### 门诊病历

预期能抽：

- 主诉
- 现病史
- 既往史
- 诊断
- 处理意见

## 7.3 P2 测试用例

- 知情同意日期晚于检查日期，提示异常。
- 报告日期早于检查日期，提示异常。
- 多资料受试者编号不一致，提示冲突。
- 必需资料缺失，提示缺失。

## 7.4 P3 测试用例

- 只将 confirmed 字段写入正式 JSON。
- 未审核字段不进入正式区。
- 多次生成保留版本。
- 当前有效版本可查询。

---

# 8. Codex 执行任务拆解

## 8.1 第一批任务：P0 后端分类与切分

### Task 1：抽离页面文本标准化模块

目标：新增 `page_text_normalizer.py`，让现有分析流程统一调用。

交付：

- 标准化函数。
- 单元测试。
- Debug 输出。

### Task 2：新增资料项类型注册表

目标：新增 `document_type_registry.py`。

交付：

- 初始 8 类资料项。
- 标题规则。
- 特征规则。
- 负向规则。

### Task 3：实现页面级分类器

目标：新增 `page_classifier.py`。

交付：

- 输入单页 OCR 文本。
- 输出分类结果。
- 支持标题、特征、负向规则打分。

### Task 4：实现 segment_builder

目标：基于页面分类结果生成资料片段。

交付：

- 连续页逻辑。
- 强标题切段。
- 多页延续。
- unknown 兜底。

### Task 5：接入现有 analyze_packet

目标：替换或增强当前 `pdf_packets.py` 中的切分逻辑。

交付：

- 原有上传识别流程可继续运行。
- 新逻辑生成更准确 segments。
- 保留 OCR 文本。

---

## 8.2 第二批任务：P0 前端人工修正

### Task 6：segment 编辑接口

目标：实现修改页码范围、资料项类型、确认状态。

交付：

- PATCH segment API。
- 参数校验。
- 操作日志。

### Task 7：segment 拆分接口

目标：支持一个片段拆多个片段。

交付：

- split API。
- 页码合法性校验。
- 原片段失效或替换。

### Task 8：segment 合并接口

目标：支持多个连续片段合并。

交付：

- merge API。
- 页码连续性校验。
- 合并后状态处理。

### Task 9：前端操作区改造

目标：在资料包识别结果页增加拆分、合并、确认。

交付：

- 拆分弹窗。
- 合并操作。
- 编辑保存。
- 状态展示。

---

## 8.3 第三批任务：P1 字段抽取

### Task 10：字段模板体系

目标：新增字段模板配置。

交付：

- 知情同意书模板。
- 交接记录表模板。
- 影像报告模板。
- 门诊病历模板。

### Task 11：字段抽取器

目标：实现规则型字段抽取。

交付：

- 日期抽取。
- key-value 抽取。
- 长文本区块抽取。
- 签字存在性初步判断。

### Task 12：字段存储

目标：新增字段结果存储。

交付：

- segment_fields 表或 JSON 字段。
- CRUD API。
- 审核状态。

### Task 13：详情页字段展示

目标：受试者详情页展示资料项字段。

交付：

- 展开行。
- 字段编辑。
- 字段确认。
- 保存刷新不丢失。

---

## 8.4 第四批任务：P2/P3 闭环

### Task 14：时间逻辑校验

目标：实现基础时间顺序规则。

交付：

- 校验规则模块。
- 详情页异常提示。

### Task 15：资料完整性检查

目标：按受试者检查资料齐全性。

交付：

- 完整性结果。
- 缺失项展示。

### Task 16：JSON 资产生成

目标：审核确认后生成结构化 JSON。

交付：

- JSON builder。
- structured_assets 表。
- 下载/查询 API。

---

# 9. 推荐开发顺序

## 第 1 阶段：P0 后端闭环

优先完成：

1. P0.1 标题强识别。
2. P0.2 文本标准化。
3. P0.3 文档类型注册表。
4. P0.4 结构特征识别。
5. P0.5 负向规则。
6. P0.6 连续页切分。
7. P0.12 调试报告。

完成标志：010005.pdf 前 12 页能正确切分。

## 第 2 阶段：P0 人工校正闭环

优先完成：

1. P0.8 拆分。
2. P0.9 合并。
3. P0.10 修改。
4. P0.11 确认冻结。

完成标志：系统自动切错时，人工能快速改正，重新识别不覆盖。

## 第 3 阶段：P1 字段抽取 MVP

优先完成：

1. P1.1 字段模板。
2. P1.2 知情同意书。
3. P1.4 医学影像报告。
4. P1.5 门诊病历。
5. P1.6 详情页展示。

完成标志：重点字段能预填、能修改、能确认。

## 第 4 阶段：P2/P3 数据资产闭环

优先完成：

1. P2.1 时间校验。
2. P2.2 资料完整性。
3. P3.1 JSON 结构。
4. P3.2 JSON 生成。

完成标志：审核确认后的结构化数据可生成 JSON 并供查询。

---

# 10. V3 验收标准

## P0 验收

- 010005.pdf 前 12 页切分正确。
- 长标题优先规则生效。
- 资料项可人工拆分、合并、修改、确认。
- 人工确认结果不会被重新识别覆盖。
- 可查看识别原因和调试报告。

## P1 验收

- 知情同意书、影像报告、门诊病历至少 3 类资料支持字段抽取。
- 字段可展示、编辑、确认。
- 签名字段以“是否存在签字”为优先，不强求识别签名文字。
- 字段结果挂在资料项下。

## P2 验收

- 能输出时间顺序异常。
- 能输出资料完整性结果。
- 能输出字段一致性冲突。

## P3 验收

- confirmed 字段可生成结构化 JSON。
- JSON 可查询、可下载、可版本管理。
- 研发侧可只读获取确认后的 JSON。

---

# 11. 风险与约束

## 11.1 OCR 质量风险

扫描质量、歪斜、手写内容会影响识别结果。

处理策略：

- P0 尽量依赖标题与结构特征。
- P1 字段只做预填。
- 人工审核为正式入库前置条件。

## 11.2 字段清单不稳定风险

领导和业务侧后续可能继续调整字段。

处理策略：

- 字段模板化。
- 字段结果 JSON 化。
- 避免频繁改 subject 主表。

## 11.3 自动化过度承诺风险

手写签名、复杂病历、医学判断不适合一开始全自动。

处理策略：

- 签名先做存在性判断。
- 病历先做文本块拆分。
- 医学含义只做“提及/疑似/无法判断”。

## 11.4 重新识别覆盖人工结果风险

处理策略：

- 人工确认结果加锁。
- 重新识别默认不覆盖。
- 操作日志可追踪。

---

# 12. 下一步立即执行清单

## 12.1 先让 Codex 检查当前代码

检查文件：

```text
backend/app/services/pdf_packets.py
backend/app/models/pdf_packet.py
backend/app/api/v1/endpoints/pdf_packets.py
frontend/src/pages/clinical-dataset/SubjectDetailPage.tsx
```

输出：

- 当前 segment 数据结构。
- 当前 analyze_packet 流程。
- 当前资料项识别规则。
- 当前前端 segment 编辑能力。
- 最小改造点。

## 12.2 先做 P0 最小闭环

第一轮代码目标：

```text
标题强识别 + 长标题优先 + 负向规则 + 连续页切分 + debug report
```

第一轮测试目标：

```text
010005.pdf 前 12 页切分正确
```

## 12.3 再做人工拆分/合并

第二轮代码目标：

```text
split API + merge API + update API + confirm lock
```

第二轮测试目标：

```text
自动分错时，人工可以修正并保存
```

## 12.4 最后接 P1 字段抽取

第三轮代码目标：

```text
字段模板 + 知情同意书字段 + 影像报告字段 + 门诊病历字段 + 详情页展示
```

---

# 13. 可直接发给 Codex 的首轮任务提示词

```text
请基于当前 clinical-data-system 仓库推进 V3 版本 PDF 资料包识别链路改造，首轮只做 P0：资料项切分准确性增强，不做字段抽取。

请先检查以下文件：
- backend/app/services/pdf_packets.py
- backend/app/models/pdf_packet.py
- backend/app/api/v1/endpoints/pdf_packets.py
- frontend/src/pages/clinical-dataset/SubjectDetailPage.tsx

当前核心问题：010005.pdf 被识别为“知情同意书 1-5 页”，但实际第 1 页才是知情同意书，第 2 页是知情同意书交接记录表，第 3 页是医学影像科检查报告单，第 4-12 页是门诊病历。现有切分逻辑不能满足 V3 要求。

首轮目标：
1. 增加页面文本标准化能力；
2. 增加资料项类型注册表；
3. 实现标题强识别，要求长标题优先，例如“知情同意书交接记录表”优先于“知情同意书”；
4. 增加结构特征识别，例如门诊病历根据主诉、现病史、既往史、诊断、处理意见识别；医学影像报告根据检查号、影像所见、诊断意见、报告医生识别；
5. 增加负向规则，避免检查报告被识别为门诊病历，避免交接记录表被识别为知情同意书；
6. 实现连续页切分逻辑，强标题开启新 segment，低置信度标记待确认；
7. 生成 analysis debug report，记录每页识别结果、命中特征、置信度、切分原因；
8. 不覆盖人工已确认 segment，预留 is_manual_locked 或等价机制。

验收目标：
010005.pdf 前 12 页至少应切为：
- 1-1 知情同意书
- 2-2 知情同意书交接记录表
- 3-3 医学影像检查报告单
- 4-12 门诊病历

请优先给出最小侵入式代码改造方案，然后直接实施。要求保留现有上传、识别、预览流程可用。
```

